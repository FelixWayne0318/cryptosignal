# Claude Project 完整数据流导入方案

**创建时间**: 2025-11-13
**核心思路**: 导入从数据获取到信号发送的完整数据流核心文件
**工作流程**: 完整数据链路 + 明确接口规范

---

## 🎯 完整数据流分析

### 从数据获取到信号发送的完整链路

```
┌─────────────────────────────────────────────────────────────┐
│ 第1层：数据获取 (Data Sources)                               │
├─────────────────────────────────────────────────────────────┤
│ sources/binance_futures_client.py  - Binance期货数据        │
│ data/unified_data_manager.py       - 统一数据管理器         │
│ data/realtime_kline_cache.py       - K线缓存               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 第2层：数据预处理 (Preprocessing)                            │
├─────────────────────────────────────────────────────────────┤
│ preprocessing/standardization.py   - 数据标准化             │
│ data/quality.py                    - 数据质量检测           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 第3层：因子计算 (Features)                                   │
├─────────────────────────────────────────────────────────────┤
│ features/fund_leading.py           - F因子v2（资金流领先）   │
│ features/trend.py                  - T因子（趋势）           │
│ features/momentum.py               - M因子（动量）           │
│ features/cvd.py                    - C因子（CVD）            │
│ features/volume.py                 - V因子（量能）           │
│ features/open_interest.py          - O因子（持仓）           │
│ features/basis.py                  - B因子（基差）           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 第4层：评分和分组 (Scoring)                                  │
├─────────────────────────────────────────────────────────────┤
│ scoring/factor_groups.py           - 因子分组（TC/VOM/B）    │
│ scoring/integrated_score.py        - 综合评分               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 第5层：过滤系统 (Gates)                                      │
├─────────────────────────────────────────────────────────────┤
│ gates/integrated_gates.py          - 四道闸门               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 第6层：统计校准 (Calibration)                                │
├─────────────────────────────────────────────────────────────┤
│ calibration/empirical_calibration.py - 经验校准器           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 第7层：v7.2集成引擎 (Pipeline)                               │
├─────────────────────────────────────────────────────────────┤
│ pipeline/analyze_symbol_v72.py     - v7.2集成分析           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 第8层：信号输出 (Outputs)                                    │
├─────────────────────────────────────────────────────────────┤
│ outputs/telegram_fmt.py            - Telegram格式化（可选） │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 完整导入清单（约30-35个文件，<3M）

### ✅ 第1类：完整数据流核心文件（必需）

#### 1. 数据获取层（6个文件）
```
ats_core/sources/
├─ binance_futures_client.py    23K  - Binance期货API
├─ binance.py                    15K  - 通用Binance接口
└─ binance_safe.py               11K  - 安全封装

ats_core/data/
├─ unified_data_manager.py       32K  - 统一数据管理
├─ realtime_kline_cache.py       24K  - K线缓存
└─ quality.py                    17K  - 数据质量检测

小计：~122K
```

#### 2. 数据预处理层（2个文件）
```
ats_core/preprocessing/
└─ standardization.py                 - 数据标准化

ats_core/data/
└─ analysis_db.py                37K  - 分析数据库（可选）

小计：~40K
```

#### 3. 因子计算层（7个文件）
```
ats_core/features/
├─ fund_leading.py                    - F因子v2
├─ trend.py                           - T因子
├─ momentum.py                        - M因子
├─ cvd.py                        22K  - C因子
├─ volume.py                          - V因子
├─ open_interest.py              22K  - O因子
└─ basis.py                           - B因子

小计：~150K
```

#### 4. 评分分组层（2个文件）
```
ats_core/scoring/
├─ factor_groups.py                   - 因子分组（TC/VOM/B）
└─ integrated_score.py                - 综合评分

小计：~20K
```

#### 5. 过滤校准层（2个文件）
```
ats_core/gates/
└─ integrated_gates.py           31K  - 四道闸门

ats_core/calibration/
└─ empirical_calibration.py      39K  - 统计校准

小计：~70K
```

#### 6. v7.2集成层（1个文件）
```
ats_core/pipeline/
└─ analyze_symbol_v72.py         37K  - v7.2集成引擎

小计：~37K
```

#### 7. 输出层（1个文件，可选）
```
ats_core/outputs/
└─ telegram_fmt.py               89K  - Telegram格式化（如果太大可排除）

小计：0-89K（可选）
```

---

### ✅ 第2类：核心依赖文件（必需）

#### 8. 配置管理（5个文件）
```
ats_core/config/
├─ threshold_config.py            7K  - 阈值配置管理
├─ factor_config.py              17K  - 因子配置管理
├─ anti_jitter_config.py          9K  - 防抖动配置
└─ __init__.py                        - 模块初始化

config/
└─ signal_thresholds.json             - 阈值配置文件

小计：~35K
```

#### 9. 工具函数（6个文件）
```
ats_core/utils/
├─ math_utils.py                  6K  - 数学工具函数
├─ cvd_utils.py                  25K  - CVD工具函数
├─ factor_normalizer.py          12K  - 因子标准化
├─ outlier_detection.py           6K  - 异常值检测
└─ __init__.py                        - 模块初始化

小计：~50K
```

#### 10. 日志模块（1个文件）
```
ats_core/
└─ logging.py                         - 日志函数（很小）

小计：<5K
```

---

### ✅ 第3类：接口和规范文档（必需）

#### 11. 接口文档（4个文件）
```
根目录/
├─ CLAUDE_PROJECT_CONTEXT.md         - 系统状态说明
├─ CLAUDE_PROJECT_INTERFACE.md       - 接口规范（新建）
└─ README.md                          - 项目说明

standards/
├─ 00_INDEX.md                        - 规范索引
└─ SYSTEM_ENHANCEMENT_STANDARD.md    - 开发规范

小计：~100K
```

---

## 📊 总容量估算

```
数据流核心文件：
  数据获取：    122K
  预处理：       40K
  因子计算：    150K
  评分分组：     20K
  过滤校准：     70K
  v7.2引擎：     37K
  输出层：     0-89K（可选）
  ──────────────────
  小计：      439-528K

核心依赖文件：
  配置管理：     35K
  工具函数：     50K
  日志模块：      5K
  ──────────────────
  小计：        ~90K

接口文档：      ~100K
──────────────────────
总计：        ~630-720K

预计Project容量占用：30-35%
```

---

## 🔌 关键接口设计

### 接口1：主入口函数（v7.2分析引擎）

**文件**: `ats_core/pipeline/analyze_symbol_v72.py`

**主要接口函数**:
```python
def analyze_with_v72_enhancements(
    result: Dict[str, Any],     # 基础分析结果
    symbol: str,                # 交易对
    klines: pd.DataFrame,       # K线数据
    oi_data: Dict,              # 持仓量数据
    cvd_series: pd.Series,      # CVD数据
    atr_now: float              # 当前ATR
) -> Dict[str, Any]:
    """
    v7.2增强分析主入口

    返回:
        {
            'F_factor': float,           # F因子v2
            'TC_group': float,           # TC组得分
            'VOM_group': float,          # VOM组得分
            'B_group': float,            # B组得分
            'weighted_score': float,     # 加权总分
            'gate_passed': bool,         # 是否通过闸门
            'calibrated_prob': float,    # 校准后概率
            ...
        }
    """
```

**依赖的仓库文件**（不在Project中）:
- `scripts/realtime_signal_scanner.py` - 调用此函数
- `ats_core/pipeline/batch_scan_optimized.py` - 批量扫描器

**如何使用**:
```python
# 在仓库代码中调用Project中的核心函数
from ats_core.pipeline.analyze_symbol_v72 import analyze_with_v72_enhancements

result_v72 = analyze_with_v72_enhancements(
    result=base_result,
    symbol="BTCUSDT",
    klines=klines_df,
    oi_data=oi_dict,
    cvd_series=cvd_series,
    atr_now=atr
)
```

---

### 接口2：数据获取接口

**文件**: `ats_core/data/unified_data_manager.py`

**主要接口类**:
```python
class UnifiedDataManager:
    """统一数据管理器"""

    def get_klines(self, symbol: str, interval: str, limit: int) -> pd.DataFrame:
        """获取K线数据"""

    def get_open_interest(self, symbol: str) -> Dict:
        """获取持仓量数据"""

    def get_cvd(self, symbol: str, lookback: int) -> pd.Series:
        """获取CVD数据"""
```

**依赖的仓库文件**:
- `scripts/realtime_signal_scanner.py` - 使用数据管理器
- `ats_core/pipeline/batch_scan_optimized.py` - 批量数据获取

---

### 接口3：配置接口

**文件**: `ats_core/config/threshold_config.py`

**主要接口类**:
```python
class ThresholdConfig:
    """阈值配置管理器"""

    def get_mature_threshold(self, key: str, default=None):
        """获取成熟币阈值"""

    def get_newcoin_threshold(self, phase: str, key: str, default=None):
        """获取新币阈值"""
```

**配置文件**: `config/signal_thresholds.json`

---

### 接口4：信号输出接口（可选）

**文件**: `ats_core/outputs/telegram_fmt.py` (如果导入)

**主要接口函数**:
```python
def render_trade_v72(result: Dict, symbol: str, ...) -> str:
    """渲染v7.2格式的Telegram消息"""
```

**依赖的仓库文件**:
- `scripts/realtime_signal_scanner.py` - 发送Telegram通知

---

## 📝 接口规范文档（新建）

### CLAUDE_PROJECT_INTERFACE.md 内容大纲

```markdown
# Claude Project 接口规范

## 1. 接口概述

本Project导入了从数据获取到信号输出的完整数据流核心文件。

### 1.1 导入范围
- ✅ 数据获取、预处理、因子计算、评分、过滤、校准、v7.2引擎
- ❌ 启动脚本、扫描器、批量处理（在仓库中）

### 1.2 接口边界
- **入口**: analyze_symbol_v72.py 的 analyze_with_v72_enhancements()
- **出口**: 返回包含信号详情的字典
- **调用方**: scripts/realtime_signal_scanner.py（在仓库中）

## 2. 主要接口函数

### 2.1 v7.2分析引擎
[详细参数说明]

### 2.2 数据管理器
[详细API说明]

### 2.3 配置管理器
[详细配置项说明]

## 3. 数据结构定义

### 3.1 分析结果结构
```python
{
    'symbol': str,
    'F_factor': float,
    'TC_group': float,
    ...
}
```

### 3.2 K线数据结构
[DataFrame schema]

## 4. 依赖关系

### 4.1 Project中的依赖
- 因子计算依赖utils/math_utils.py
- 闸门依赖config/threshold_config.py

### 4.2 仓库中的依赖
- realtime_signal_scanner.py调用v7.2引擎
- batch_scan_optimized.py提供批量扫描

## 5. 使用示例

### 5.1 修改F因子计算
[完整示例]

### 5.2 调整闸门阈值
[完整示例]

### 5.3 添加新因子
[完整示例]

## 6. 常见问题

### 6.1 如何调试数据流？
### 6.2 如何验证修改？
### 6.3 如何处理依赖？
```

---

## ⚠️ 可能遗漏的问题

### 1. 类型定义和常量
```
可能需要导入：
- ats_core/types.py (如果存在)
- ats_core/constants.py (如果存在)
- ats_core/enums.py (如果存在)
```

### 2. 异常处理
```
可能需要导入：
- ats_core/exceptions.py (如果存在)
- 自定义异常类
```

### 3. 数据结构定义
```
如果有专门的数据类：
- ats_core/models/signal.py
- ats_core/models/market_data.py
```

### 4. 配置文件的完整性
```
除了signal_thresholds.json，可能还需要：
- config/factor_weights.json
- config/newcoin_stages.json
```

### 5. 初始化文件
```
所有目录的__init__.py文件：
- ats_core/__init__.py
- ats_core/features/__init__.py
- ats_core/scoring/__init__.py
- 等等
```

### 6. 测试数据
```
如果需要测试修改：
- tests/fixtures/ 中的测试数据
- tests/test_helpers.py
```

---

## 🔧 .claudeignore 配置（完整数据流版）

```bash
# 版本控制
.git/
__pycache__/
*.pyc

# 运行时数据
data/
reports/
logs/
*.log
*.db

# 归档测试
archived/
tests/
diagnose/

# 启动脚本（在仓库中，不需要导入）
setup.sh
auto_restart.sh
scripts/realtime_signal_scanner.py
scripts/init_databases.py
scripts/batch*.py

# ats_core中排除的模块
ats_core/pipeline/analyze_symbol.py        # 旧版
ats_core/pipeline/batch_scan_optimized.py  # 批量扫描器（在仓库中）
ats_core/execution/                        # 执行层
ats_core/publishing/                       # 发布层
ats_core/analysis/                         # 分析工具
ats_core/monitoring/                       # 监控
ats_core/shadow/                           # 影子模式
ats_core/tools/                            # 反过拟合工具
ats_core/factors_v2/                       # 旧版因子
ats_core/modulators/                       # 调制器
ats_core/rl/                               # 强化学习
ats_core/risk/                             # 风控
ats_core/streaming/                        # 流式处理

# 如果telegram_fmt太大，也可排除
# ats_core/outputs/telegram_fmt.py

# 详细文档（需要时查阅）
docs/
standards/01_SYSTEM_OVERVIEW.md
standards/02_ARCHITECTURE.md
standards/03_VERSION_HISTORY.md
standards/deployment/
standards/specifications/

# 其他配置
config/binance_credentials.json
config/telegram.json
```

---

## 📋 导入验证清单

### Project中应该有：

**数据流核心**：
- [x] sources/binance_futures_client.py
- [x] data/unified_data_manager.py
- [x] data/realtime_kline_cache.py
- [x] preprocessing/standardization.py
- [x] features/ (7个因子)
- [x] scoring/ (2个文件)
- [x] gates/integrated_gates.py
- [x] calibration/empirical_calibration.py
- [x] pipeline/analyze_symbol_v72.py

**核心依赖**：
- [x] config/threshold_config.py
- [x] config/signal_thresholds.json
- [x] utils/math_utils.py
- [x] logging.py

**接口文档**：
- [x] CLAUDE_PROJECT_CONTEXT.md
- [x] CLAUDE_PROJECT_INTERFACE.md
- [x] standards/SYSTEM_ENHANCEMENT_STANDARD.md

### Project中不应该有：
- [ ] scripts/realtime_signal_scanner.py
- [ ] ats_core/pipeline/batch_scan_optimized.py
- [ ] setup.sh

### 容量占用：
- [ ] 进度条 < 40%（约630-720K）

---

## 🚀 快速开始

### 步骤1：创建接口规范文档（5分钟）
参考上面的大纲创建 `CLAUDE_PROJECT_INTERFACE.md`

### 步骤2：应用配置（10秒）
```bash
cp .claudeignore.dataflow .claudeignore
git add .claudeignore CLAUDE_PROJECT_INTERFACE.md
git commit -m "feat: 应用完整数据流导入配置"
git push
```

### 步骤3：导入Project（2分钟）
1. Claude.ai → Create Project "CryptoSignal v7.2 DataFlow"
2. Add from GitHub
3. 让.claudeignore自动过滤

### 步骤4：验证接口（5分钟）
```
"请阅读 CLAUDE_PROJECT_INTERFACE.md，
确认你理解了完整数据流和接口规范。

然后追踪一次完整的信号生成流程：
从数据获取 → 因子计算 → 评分 → 闸门 → 输出"
```

---

**创建时间**: 2025-11-13
**系统版本**: v7.2.36
**推荐程度**: ⭐⭐⭐⭐⭐（最完整方案）

**这才是完整的数据流方案！** 🎯
