# CryptoSignal 版本历史

> ⚠️ **重要**：本文档已纳入标准化部署规范体系
> 标准规范：[standards/DEPLOYMENT_STANDARD.md](standards/DEPLOYMENT_STANDARD.md)
> 快速参考：[QUICK_DEPLOY.md](QUICK_DEPLOY.md)

---

## 📋 NEWCOIN_SPEC.md 合规性审查 - 2025-11-02

### 🎯 审查目的

对比当前v6.3.2实现与 `standards/specifications/NEWCOIN.md` 规范要求，识别合规差距，制定实施路线图。

### 📊 合规性矩阵

| # | 规范要求 | 规范章节 | 当前实现 | 合规状态 | 优先级 | 实施难度 |
|---|---------|---------|---------|---------|--------|---------|
| **1. 进入与回切标准** |
| 1.1 | 进入条件：`bars_1h < 400` | § 1 | ✅ 已实现 | ✅ 完全合规 | - | - |
| 1.2 | 进入条件：`since_listing < 14d` | § 1 | ⚠️ 用coin_age近似 | ⚠️ 部分合规 | P1 | 中 |
| 1.3 | 进入条件：`!has_OI/funding` | § 1 | ❌ 未实现 | ❌ 不合规 | P2 | 低 |
| 1.4 | 回切条件：`bars_1h ≥ 400` 且 OI/funding连续≥3d | § 1 | ❌ 硬切换 | ❌ 不合规 | P2 | 中 |
| 1.5 | 渐变切换：48h线性混合 | § 1 | ❌ 未实现 | ❌ 不合规 | P2 | 高 |
| **2. 数据流与粒度** |
| 2.1 | 数据源：Binance USDT-M | § 2 | ✅ 已实现 | ✅ 完全合规 | - | - |
| 2.2 | REST：1m/5m/15m/1h klines | § 2 | ❌ 仅1h/4h | ❌ 不合规 | **P0** | 高 |
| 2.3 | WS：kline_1m/5m/15m | § 2 | ❌ 无WS | ❌ 不合规 | **P0** | 高 |
| 2.4 | WS：aggTrade | § 2 | ❌ 无WS | ❌ 不合规 | **P0** | 高 |
| 2.5 | WS：depth@100ms | § 2 | ❌ 无WS | ❌ 不合规 | P1 | 高 |
| 2.6 | WS：markPrice@1s | § 2 | ❌ 无WS | ❌ 不合规 | P1 | 中 |
| 2.7 | 锚点：AVWAP_from_listing | § 2 | ❌ 未实现 | ❌ 不合规 | **P0** | 中 |
| **3. 新币专用因子** |
| 3.1 | T_new（ZLEMA_1m, HL=5） | § 3 | ❌ 用标准T | ❌ 不合规 | **P0** | 中 |
| 3.2 | M_new（ZLEMA_5m, HL=8） | § 3 | ❌ 用标准M | ❌ 不合规 | **P0** | 中 |
| 3.3 | S_new（EWMA_15m, HL=20） | § 3 | ❌ 用标准S | ❌ 不合规 | **P0** | 中 |
| 3.4 | V_new | § 3 | ❌ 用标准V | ❌ 不合规 | P1 | 低 |
| 3.5 | C_new | § 3 | ❌ 用标准C | ❌ 不合规 | P1 | 低 |
| 3.6 | O_new（无OI时权重0） | § 3 | ⚠️ 固定权重 | ⚠️ 部分合规 | P1 | 低 |
| 3.7 | Q_sig_new | § 3 | ❌ 用标准Q | ❌ 不合规 | P1 | 中 |
| 3.8 | 权重：T22/M15/S15/V16/C20/O8/Q4 | § 3 | ❌ 统一权重 | ❌ 不合规 | P1 | 低 |
| **4. 点火-成势-衰竭模型** |
| 4.1 | 点火条件（≥3条成立） | § 4 | ❌ 用Prime判定 | ❌ 不合规 | **P0** | 高 |
| 4.2 | 成势确认（多时间框架斜率） | § 4 | ❌ 未实现 | ❌ 不合规 | **P0** | 高 |
| 4.3 | 衰竭/反转检测 | § 4 | ❌ 未实现 | ❌ 不合规 | **P0** | 高 |
| 4.4 | CVD（累计成交量差） | § 4 | ❌ 未实现 | ❌ 不合规 | **P0** | 中 |
| 4.5 | OBI（订单簿失衡） | § 4 | ❌ 未实现 | ❌ 不合规 | P1 | 中 |
| 4.6 | RVOL（相对成交量） | § 4 | ❌ 未实现 | ❌ 不合规 | P1 | 低 |
| **5. 调节器参数（新币特例）** |
| 5.1 | F初期置0.5（中性） | § 5 | ❌ 用标准F | ❌ 不合规 | P1 | 低 |
| 5.2 | I降权（15m-1h粗相关） | § 5 | ❌ 用标准I | ❌ 不合规 | P2 | 低 |
| 5.3 | 温度参数：T0=60, Tmin=40, Tmax=95 | § 5 | ❌ 统一参数 | ❌ 不合规 | P2 | 低 |
| 5.4 | 成本/门槛参数：λF/λI/θF/θI/φF/φI | § 5 | ❌ 统一参数 | ❌ 不合规 | P2 | 低 |
| 5.5 | 概率收缩：`w_eff = min(1, bars_1h/400)` | § 5 | ❌ 未实现 | ❌ 不合规 | P1 | 低 |
| **6. 执行与闸门** |
| 6.1 | 硬闸：impact ≤ 7/8 bps | § 6 | ❌ 统一闸门 | ❌ 不合规 | P1 | 中 |
| 6.2 | 硬闸：spread ≤ 35/38 bps | § 6 | ❌ 统一闸门 | ❌ 不合规 | P1 | 中 |
| 6.3 | 硬闸：`|OBI| ≤ 0.30/0.33` | § 6 | ❌ 未实现 | ❌ 不合规 | P1 | 中 |
| 6.4 | 硬闸：DataQual ≥ 0.90/0.88 | § 6 | ⚠️ 基础实现 | ⚠️ 部分合规 | P1 | 低 |
| 6.5 | 入场：回撤接力（AVWAP/ZLEMA_5m锚点） | § 6 | ❌ 统一策略 | ❌ 不合规 | P1 | 中 |
| 6.6 | SL/TP：1m/5m颗粒 | § 6 | ❌ 统一颗粒 | ❌ 不合规 | P1 | 低 |
| 6.7 | TTL：2-4h | § 6 | ❌ 统一TTL | ❌ 不合规 | P2 | 低 |
| 6.8 | Prime窗口：0-3m冷启动，3-8m首批，8-15m主力 | § 6 | ❌ 未实现 | ❌ 不合规 | P1 | 中 |
| **7. WS稳定性** |
| 7.1 | 组合流订阅（3-5个连接） | § 7 | ❌ 无WS | ❌ 不合规 | **P0** | 高 |
| 7.2 | 指数回退+抖动重连 | § 7 | ❌ 无WS | ❌ 不合规 | **P0** | 高 |
| 7.3 | REST深度快照对账 | § 7 | ❌ 无WS | ❌ 不合规 | P1 | 中 |
| 7.4 | 心跳缺失 → DataQual降级 | § 7 | ❌ 未实现 | ❌ 不合规 | P1 | 低 |

### 📈 合规性统计

| 合规状态 | 数量 | 占比 | 说明 |
|---------|------|------|------|
| ✅ 完全合规 | 2 | 4.3% | bars_1h判断、数据源 |
| ⚠️ 部分合规 | 4 | 8.5% | since_listing近似、O权重、DataQual基础、质量补偿 |
| ❌ 不合规 | 41 | 87.2% | 大部分核心功能未实现 |
| **总计** | **47** | **100%** | - |

### 🔴 关键缺失功能（P0级）

#### 1. 数据粒度架构性缺陷 ⚠️ **CRITICAL**

**问题**: 所有币种使用相同的1h/4h REST数据，新币无法获取规范要求的1m/5m/15m数据

**影响**:
- 无法实现ZLEMA_1m/5m因子（§3.1, §3.2）
- 无法捕捉分钟级波动（新币核心特性）
- 无法实现点火-成势模型（§4，依赖1m速度、aggTrade）
- 无法实现AVWAP锚点（§2.7，需从listing第一分钟开始）

**根本原因**:
新币检测发生在数据获取**之后**（analyze_symbol.py:147-238），此时已用标准粒度拉取数据，无法追溯

**实施要求**:
```
阶段0（数据获取前）: 快速预判是否为新币
  ├─ 调用exchangeInfo获取listingTime
  └─ 判断: bars_1h_approx < 400 或 since_listing < 14d

阶段1（数据获取）: 根据预判结果分别获取
  ├─ 新币: 1m/5m/15m/1h klines + WS订阅
  └─ 成熟币: 1h/4h/15m/1d klines

阶段2（精准判断）: 基于完整数据重新确认
  └─ 使用len(k1h)精确判断

阶段3（因子计算）: 使用币种专用因子
  ├─ 新币: T_new(ZLEMA_1m), M_new(ZLEMA_5m), S_new(EWMA_15m)
  └─ 成熟币: T(EMA_1h), M(EMA_4h), S(EMA_4h)

阶段4（判定逻辑）: 使用币种专用模型
  ├─ 新币: 点火-成势-衰竭模型
  └─ 成熟币: 标准Prime判定
```

**文件影响**:
- `ats_core/pipeline/analyze_symbol.py`: 需重构数据获取流程（Lines 958-1033前置预判）
- `ats_core/pipeline/batch_scan_optimized.py`: 同步修改
- 新增: `ats_core/factors/newcoin_factors.py`（T_new/M_new/S_new实现）
- 新增: `ats_core/models/point_fire_momentum.py`（点火-成势模型）

#### 2. 点火-成势-衰竭模型缺失 ⚠️ **CRITICAL**

**问题**: 当前使用标准Prime判定（prime_strength阈值），与新币快反特性不匹配

**规范要求（§4）**:
- **点火（≥3条成立）**:
  - `(P-AVWAP)/ATR_1m ≥ 0.8`
  - `speed ≥ 0.25 ATR/min (≥2min)`
  - `agg_buy ≥ 0.62`（多）或 `agg_sell ≥ 0.62`（空）
  - `OBI10 ≥ 0.05`（多）或 `≤ -0.05`（空）
  - `RVOL_10m ≥ 3.0`（不足则 `RVOL_5m ≥ 2.0`）
  - `slope_CVD > 0`（多）或 `< 0`（空）
- **成势确认**: 1m/5m斜率同向，15m ≥ 0
- **衰竭/反转**: 失锚 + CVD翻转；speed<0连续2-3根；OBI反号且对侧agg≥0.60；qvol/ATR > 0.6

**当前实现**:
仅使用prime_strength（基于9因子加权），缺少时间序列动态追踪

**实施要求**:
```python
# 新增模块: ats_core/models/point_fire_momentum.py
class PointFireMomentumModel:
    def check_ignition(self, k1m, k5m, avwap, atr_1m) -> Tuple[bool, List[str]]:
        """检测点火条件（≥3条成立）"""
        conditions_met = []

        # 1. 价格偏离锚点
        if (price - avwap) / atr_1m >= 0.8:
            conditions_met.append("price_divergence")

        # 2. 速度检测（需aggTrade数据）
        speed = self._calc_speed(k1m, atr_1m)  # ATR/min
        if speed >= 0.25 and self._speed_duration >= 2:
            conditions_met.append("speed")

        # 3. 主动买入占比（需aggTrade数据）
        agg_buy = self._calc_agg_buy(k1m)
        if agg_buy >= 0.62:
            conditions_met.append("agg_buy")

        # 4-6: OBI, RVOL, CVD...

        return len(conditions_met) >= 3, conditions_met

    def check_momentum(self, k1m, k5m, k15m) -> bool:
        """成势确认：多时间框架斜率同向"""
        slope_1m = self._calc_slope(k1m, method="ZLEMA", hl=5)
        slope_5m = self._calc_slope(k5m, method="ZLEMA", hl=8)
        slope_15m = self._calc_slope(k15m, method="EWMA", hl=20)

        # 1m/5m同向，15m ≥ 0
        return (slope_1m * slope_5m > 0) and (slope_15m >= 0)

    def check_exhaustion(self, ...) -> Tuple[bool, str]:
        """衰竭/反转检测"""
        # 失锚 + CVD翻转 / speed<0连续 / OBI反号 / qvol/ATR > 0.6
        ...
```

**依赖数据**:
- aggTrade（计算agg_buy/sell、speed）
- depth（计算OBI）
- AVWAP锚点

#### 3. WS实时数据流缺失 ⚠️ **CRITICAL**

**问题**: 当前仅使用REST轮询（每5分钟），无法实时捕捉新币快速波动

**规范要求（§2, §7）**:
- WS订阅: kline_1m/5m/15m、aggTrade、depth@100ms、markPrice@1s
- 组合流: 3-5个连接（降低单连接压力）
- 稳定性: 指数回退重连、定时REST对账、心跳监控

**实施要求**:
```python
# 新增模块: ats_core/data_feeds/ws_newcoin.py
class NewCoinWSFeed:
    def __init__(self, symbol: str):
        self.streams = [
            f"{symbol.lower()}@kline_1m",
            f"{symbol.lower()}@kline_5m",
            f"{symbol.lower()}@kline_15m",
            f"{symbol.lower()}@aggTrade",
            f"{symbol.lower()}@depth@100ms",
            f"{symbol.lower()}@markPrice@1s",
        ]
        self.connections = []  # 3-5个连接
        self.reconnect_backoff = ExponentialBackoff(...)

    async def subscribe(self):
        # 分配流到多个连接（组合流）
        conn1 = [kline_1m, kline_5m, kline_15m]
        conn2 = [aggTrade]
        conn3 = [depth, markPrice]
        ...

    async def handle_message(self, msg):
        # 更新本地缓存（k1m, k5m, k15m, agg_trades, depth）
        ...

    async def reconcile_with_rest(self):
        # 定期REST深度快照对账
        ...
```

### 🟡 次要缺失功能（P1级）

#### 4. 真实since_listing获取

**当前实现**: 用coin_age_days近似（基于K线时间戳差）

**规范要求（§1）**: 精确的since_listing时间（交易所API）

**实施方案**:
```python
# ats_core/data_feeds/exchange_info.py
async def get_listing_time(symbol: str) -> int:
    """获取上币时间（毫秒时间戳）"""
    info = await fetch_exchange_info(symbol)
    # Binance exchangeInfo可能包含onboardDate字段
    # 若无，回退到首根K线时间戳
    return info.get('onboardDate') or first_kline_timestamp
```

#### 5. 新币专用权重与参数

**规范要求（§3, §5）**:
- 权重: T22/M15/S15/V16/C20/O8/Q4（无OI时按比例归一）
- F初期置0.5（中性），稳定≥3d再启用
- 概率收缩: `P̃ = 0.5 + w_eff*(P-0.5)`，`w_eff = min(1, bars_1h/400)`

**实施方案**: 在analyze_symbol.py新币分支中使用专用参数配置

#### 6. Prime时间窗口限制

**规范要求（§6）**:
- 0-3m: 冷启动仅Watch
- 3-8m: 可能首批Prime
- 8-15m: 主力窗口

**实施方案**:
```python
minutes_since_listing = (current_time - listing_time) / 60000
if minutes_since_listing < 3:
    signal_type = "Watch"  # 强制Watch
elif minutes_since_listing < 8:
    signal_type = "Prime" if meets_higher_threshold else "Watch"
else:  # 8-15m主力窗口
    signal_type = "Prime" if meets_standard_threshold else "Watch"
```

### 📋 实施路线图（4阶段）

#### Phase 1: 当前v6.3.2 ✅ **COMPLETED**

**已实现**:
- ✅ 新币判断: bars_1h < 400 或 coin_age_days < 14（§1部分合规）
- ✅ 币种特定阈值: prime_strength 35/32/28/25
- ✅ 质量评分补偿: 10-13%（减轻数据惩罚）

**限制**:
- ⚠️ 仅为权宜之计，使用统一1h/4h数据和标准因子
- ⚠️ 通过提高阈值来补偿粒度不足

**合规度**: **12.8%**（6/47项）

---

#### Phase 2: 数据流架构改造 🔴 **HIGH PRIORITY**

**目标**: 解决P0级数据粒度问题，实现新币专用数据流

**实施步骤**:

1. **阶段0预判（数据获取前）**
   - 修改: `ats_core/pipeline/analyze_symbol.py`
   - 在数据获取前（Line 958之前）插入快速预判逻辑
   - 调用exchangeInfo获取listing_time
   - 判断: `bars_approx < 400` 或 `since_listing < 14d`

2. **新币数据获取模块**
   - 新增: `ats_core/data_feeds/newcoin_data.py`
   - REST获取: 1m/5m/15m/1h klines（最近400根）
   - 计算AVWAP_from_listing（从listing第一分钟开始）
   - 返回: k1m, k5m, k15m, k1h, avwap, listing_time

3. **WS实时订阅（初版）**
   - 新增: `ats_core/data_feeds/ws_newcoin.py`
   - 订阅kline_1m/5m/15m（基础版，不含aggTrade/depth）
   - 实现指数回退重连
   - 心跳监控 → DataQual动态调整

4. **数据获取流程重构**
   ```python
   # analyze_symbol.py 新结构
   async def analyze_symbol_v7(symbol, params, ...):
       # 🔧 Phase 2修改点
       # 阶段0: 预判（数据获取前）
       is_new_coin_likely = await quick_newcoin_check(symbol)

       # 阶段1: 分别获取数据
       if is_new_coin_likely:
           data = await fetch_newcoin_data(symbol)  # 1m/5m/15m/1h
           k1m, k5m, k15m, k1h = data['k1m'], data['k5m'], ...
           avwap = data['avwap']
       else:
           data = await fetch_standard_data(symbol)  # 1h/4h/15m/1d
           k1h, k4h, k15m, k1d = data['k1'], data['k4'], ...

       # 阶段2: 精准判断
       bars_1h = len(k1h)
       is_new_coin = (bars_1h < 400) or (since_listing < 14d)

       # 阶段3-4: 因子计算和判定（继续在Phase 3实现）
       ...
   ```

**预期合规度提升**: 12.8% → **40%** (+19项)

**关键文件**:
- `ats_core/pipeline/analyze_symbol.py`: 重构数据获取流程
- `ats_core/data_feeds/newcoin_data.py`: 新增
- `ats_core/data_feeds/ws_newcoin.py`: 新增
- `ats_core/pipeline/batch_scan_optimized.py`: 同步修改

**估算工作量**: 3-5天（含测试）

---

#### Phase 3: 新币专用因子与模型 🔴 **HIGH PRIORITY**

**目标**: 实现ZLEMA因子和点火-成势-衰竭模型

**实施步骤**:

1. **新币专用因子实现**
   - 新增: `ats_core/factors/newcoin_factors.py`
   ```python
   def calc_T_new(k1m, hl=5) -> Tuple[float, dict]:
       """趋势因子（ZLEMA_1m斜率）"""
       zlema = calc_zlema(k1m, halflife=hl)
       slope = standardize_slope(zlema)
       return slope, {"method": "ZLEMA_1m", "hl": hl}

   def calc_M_new(k5m, hl=8) -> Tuple[float, dict]:
       """动量因子（ZLEMA_5m斜率）"""
       ...

   def calc_S_new(k15m, hl=20) -> Tuple[float, dict]:
       """强度因子（EWMA_15m斜率）"""
       ...
   ```

2. **点火-成势-衰竭模型**
   - 新增: `ats_core/models/point_fire_momentum.py`
   - 实现3阶段检测逻辑（见上文§2详细设计）
   - 依赖: k1m, k5m, k15m, avwap, atr_1m

3. **新币专用权重配置**
   - 修改: `config/params.json`
   ```json
   {
     "weights_newcoin": {
       "T": 22, "M": 15, "S": 15,
       "V": 16, "C": 20, "O": 8, "Q": 4,
       "F": 0, "I": 0
     }
   }
   ```

4. **在analyze_symbol中集成**
   ```python
   # analyze_symbol.py 阶段3-4
   if is_new_coin:
       # 使用新币因子
       T, T_meta = calc_T_new(k1m, hl=5)
       M, M_meta = calc_M_new(k5m, hl=8)
       S, S_meta = calc_S_new(k15m, hl=20)

       # 使用新币权重
       weights = params['weights_newcoin']

       # 使用点火-成势模型判定
       model = PointFireMomentumModel()
       is_ignition, conditions = model.check_ignition(k1m, k5m, avwap, atr_1m)
       is_momentum = model.check_momentum(k1m, k5m, k15m)
       is_prime = is_ignition and is_momentum
   else:
       # 使用标准因子和Prime判定
       ...
   ```

**预期合规度提升**: 40% → **65%** (+12项)

**关键文件**:
- `ats_core/factors/newcoin_factors.py`: 新增
- `ats_core/models/point_fire_momentum.py`: 新增
- `ats_core/pipeline/analyze_symbol.py`: 集成新币分支
- `config/params.json`: 新币专用配置

**估算工作量**: 4-6天（含测试）

---

#### Phase 4: 完整新币通道（生产级） 🟡 **MEDIUM PRIORITY**

**目标**: 实现规范全部要求，达到90%+合规度

**实施步骤**:

1. **WS完整订阅（aggTrade + depth）**
   - 扩展: `ats_core/data_feeds/ws_newcoin.py`
   - 新增aggTrade处理（计算agg_buy/sell、speed）
   - 新增depth@100ms处理（计算OBI）
   - 实现组合流（3-5个连接）
   - REST深度快照对账

2. **点火模型完整实现（依赖WS数据）**
   - 扩展: `ats_core/models/point_fire_momentum.py`
   - 添加speed检测（需aggTrade）
   - 添加agg_buy/sell检测（需aggTrade）
   - 添加OBI检测（需depth）
   - 添加RVOL检测
   - 添加CVD检测

3. **48h渐变切换机制**
   - 新增: `ats_core/pipeline/transition_manager.py`
   - 状态记录: 回切开始时间
   - 线性混合: `w = (elapsed_hours / 48)`
   - 混合内容: 权重、温度、阈值、TTL

4. **新币专用执行闸门**
   - 修改: `ats_core/gates/integrated_gates.py`
   - 新币硬闸: impact≤7/8bps, spread≤35/38bps, OBI≤0.30/0.33, DataQual≥0.90/0.88
   - Prime时间窗口: 0-3m/3-8m/8-15m分段逻辑
   - Room检测: R*·ATR_1m

5. **新币专用调节器参数**
   - F初期置0.5（稳定≥3d再启用）
   - I降权（15m-1h粗相关）
   - 温度/成本/门槛专用参数（§5完整参数表）
   - 概率收缩: `w_eff = min(1, bars_1h/400)`

6. **独立新币pipeline（可选）**
   - 新增: `scripts/newcoin_scanner.py`
   - 完全独立的扫描器（与标准通道隔离）
   - 专用配置: `config/params_newcoin.json`

**预期合规度提升**: 65% → **90%+** (+13项)

**关键文件**:
- `ats_core/data_feeds/ws_newcoin.py`: 扩展aggTrade/depth
- `ats_core/models/point_fire_momentum.py`: 完整6条点火条件
- `ats_core/pipeline/transition_manager.py`: 新增
- `ats_core/gates/integrated_gates.py`: 新币专用闸门
- `config/params_newcoin.json`: 完整新币参数表

**估算工作量**: 7-10天（含测试）

---

### 🎯 推荐实施顺序

**立即开始（v6.4）**: Phase 2 - 数据流架构改造
- **理由**: 解决架构性缺陷，是后续工作基础
- **优先级**: P0（阻塞其他新币功能）
- **影响**: 合规度12.8% → 40%

**后续开始（v6.5）**: Phase 3 - 新币因子与模型
- **理由**: 在Phase 2基础上实现新币专用逻辑
- **优先级**: P0（核心差异化）
- **影响**: 合规度40% → 65%

**长期规划（v7.0）**: Phase 4 - 完整新币通道
- **理由**: 生产级功能完善
- **优先级**: P1（优化）
- **影响**: 合规度65% → 90%+

### 📌 当前状态总结

**v6.3.2已完成**:
- ✅ 新币判断标准（bars_1h < 400）
- ✅ 币种特定阈值（35/32/28/25）
- ✅ 质量评分补偿（10-13%）

**合规度**: **12.8%**（6/47项）

**关键限制**:
- ⚠️ 使用统一1h/4h数据（无法捕捉分钟级波动）
- ⚠️ 使用标准因子（非ZLEMA_1m/5m）
- ⚠️ 使用标准Prime判定（非点火-成势模型）

**结论**: **当前v6.3.2是Phase 1的权宜之计**，通过提高阈值和质量补偿来部分解决问题，但根本性的架构改造需要Phase 2-4。

---

## 📋 版本更新 (v6.3.1 → v6.3.2) - 2025-11-02

### 🎯 核心问题：新币全流程逻辑冲突修复

**发现严重Bug**: 新币专用阈值定义但未实际生效，导致新币和成熟币使用完全相同的Prime判定标准

### 🐛 识别的关键问题

#### Bug #1: 新币阈值定义但未使用
- 定义了prime_prob_min等新币专用阈值（0.70/0.65/0.63 vs成熟币0.62）
- 但Prime判定固定用 `prime_strength >= 25`（所有币种相同）
- 这些变量只在rejection_reason中使用，不影响实际判定

#### Bug #2: 新币质量评分不公平惩罚
- `_calc_quality()`对K线<100的币种惩罚15%
- 新币天然数据少，被不公平惩罚
- 压低概率和prime_strength，难以通过判定

#### Bug #3: 新币和成熟币共用因子/权重
- 所有币种使用相同的T/M/S/V/C/O/L/B/Q因子（基于1h/4h）
- 规范要求新币用T_new/M_new（基于ZLEMA_1m/5m）
- 当前1h/4h粒度无法捕捉新币快速波动

### 🔧 修复方案

#### 修复 #1: 实现币种特定prime_strength阈值
```python
if is_ultra_new:
    prime_strength_threshold = 35  # +40%要求
elif is_phaseA:
    prime_strength_threshold = 32  # +28%要求
elif is_phaseB:
    prime_strength_threshold = 28  # +12%要求
else:
    prime_strength_threshold = 25  # 成熟币标准

is_prime = (prime_strength >= prime_strength_threshold)
```
- 文件: analyze_symbol.py:690-700, 766

#### 修复 #2: 新币质量评分补偿
```python
if is_new_coin and len(k1) < 100:
    if is_ultra_new:
        quality_score = min(1.0, quality_score / 0.85 * 0.90)  # 仍保留10%惩罚
    elif is_phaseA:
        quality_score = min(1.0, quality_score / 0.85 * 0.88)  # 保留12%惩罚
    elif is_phaseB:
        quality_score = min(1.0, quality_score / 0.85 * 0.87)  # 保留13%惩罚
```
- 文件: analyze_symbol.py:552-572

#### 修复 #3: 更新rejection_reason显示
- 显示实际使用的prime_strength_threshold
- 显示币种分类（coin_phase），便于诊断

### 📊 冲突解决分析

| 冲突 | 原问题 | 解决方案 |
|------|--------|---------|
| **阈值脱节** | 定义了但未使用 | 引入prime_strength_threshold，实际生效 |
| **质量惩罚** | 新币天然数据少被惩罚15% | 适度补偿到10-13%，仍保留风险提示 |
| **因子不匹配** | 用1h/4h因子，无法捕捉分钟级波动 | ⚠️ 提高阈值作为补偿（权宜之计）<br>⏸️ 长期需独立新币通道 |

### 🎯 新币全流程逻辑（修复后）

```
┌──────────────────────────────────────────────┐
│ 1. 新币判断 (v6.3.1)                          │
│    bars_1h < 400 或 coin_age_days < 14       │
├──────────────────────────────────────────────┤
│ 2. 数据要求调整                               │
│    ultra:10, phaseA:30, phaseB:50, mature:50 │
├──────────────────────────────────────────────┤
│ 3. 因子计算                                   │
│    统一T/M/S/V/C/O/L/B/Q（1h/4h粒度）         │
│    ⚠️ 待改进：需1m/5m粒度新币专用因子          │
├──────────────────────────────────────────────┤
│ 4. 质量评分 (v6.3.2新增)                     │
│    新币获得10-13%补偿（仍保留风险惩罚）       │
├──────────────────────────────────────────────┤
│ 5. Prime判定 (v6.3.2修复)                    │
│    币种特定阈值：35/32/28/25                  │
│    - ultra_new: 35分（+40%要求）             │
│    - phaseA: 32分（+28%要求）                │
│    - phaseB: 28分（+12%要求）                │
│    - mature: 25分（标准）                    │
├──────────────────────────────────────────────┤
│ 6. 拒绝原因跟踪                               │
│    显示实际阈值和币种分类                     │
└──────────────────────────────────────────────┘
```

### 📊 修复效果

**v6.3.1（修复前）**:
- Prime判定: 所有币种用25分 ❌
- 新币阈值: 定义但未生效 ❌
- 质量评分: 新币被惩罚15% ❌

**v6.3.2（修复后）**:
- Prime判定: 币种特定（35/32/28/25）✅
- 新币阈值: 实际生效（更严格）✅
- 质量评分: 适度补偿（10-13%）✅

---

## 📋 版本更新 (v6.3 → v6.3.1) - 2025-11-02

### 🎯 核心改进：新币判断规范符合性修改

**问题**: v6.3之前的新币判断逻辑不符合 `standards/specifications/NEWCOIN.md` 规范标准

### 🔧 规范符合性修改

#### 修改 #1: 新币判断标准对齐规范

**规范要求 (NEWCOIN_SPEC.md § 1)**:
- 进入新币通道: `since_listing < 14d` 或 `bars_1h < 400` 或 `!has_OI/funding`
- 回切标准通道: `bars_1h ≥ 400` 且 OI/funding连续≥3d，或 `since_listing ≥ 14d`

**旧实现（不符合规范）**:
```python
ultra_new: coin_age_hours ≤ 24
phaseA: coin_age_days ≤ 7
phaseB: coin_age_days ≤ 30  # ❌ 应为14天
# ❌ 未使用bars_1h判断
```

**新实现（符合规范）**:
```python
# 主判断条件: bars_1h < 400 （符合规范）
if bars_1h < 400:
    is_new_coin = True
    # 内部细分
    if bars_1h < 24: newcoin_ultra
    elif bars_1h < 168: newcoin_phaseA
    else: newcoin_phaseB  # 7d - 16.7d
# 辅助条件: coin_age_days < 14 （符合规范）
elif coin_age_days < 14:
    is_new_coin = True
else:
    is_new_coin = False  # 成熟币
```

**文件**:
- `ats_core/pipeline/analyze_symbol.py:165-238`
- `ats_core/pipeline/batch_scan_optimized.py:480-522`

#### 修改 #2: 添加bars_1h计数判断

**问题**: 之前只使用时间戳差值，没有使用K线数量作为判断依据

**修复**: 添加 `bars_1h = len(k1)` 计数逻辑，并作为主判断条件

**效果**:
- ✅ 符合规范的 `bars_1h < 400` 新币进入条件
- ✅ 14天阈值替代30天
- ✅ 数据受限检测改为 `bars_1h >= 200`（不再用coin_age_hours）

### 📋 当前限制与完整新币通道 Roadmap

**⚠️ 当前实现为简化版**，符合规范的判断标准但缺少完整新币通道功能：

| 功能 | 规范要求 | 当前实现 | 状态 |
|------|---------|---------|------|
| **进入/回切判断** | bars_1h < 400 或 since_listing < 14d | ✅ bars_1h < 400 <br> ⚠️ coin_age_days < 14（近似） | 部分符合 |
| **渐变切换** | 48h线性混合（权重/温度/阈值） | ❌ 硬切换 | 未实现 |
| **数据粒度** | 1m/5m/15m/1h (WS实时) | ❌ 1h/4h (REST) | 未实现 |
| **新币专用因子** | T_new/M_new/S_new (ZLEMA_1m/5m) | ❌ 标准因子 | 未实现 |
| **点火-成势-衰竭** | 非线性联立模型 | ❌ 标准Prime判定 | 未实现 |
| **执行标准** | 更严（impact≤7bps, spread≤35bps） | ❌ 标准执行标准 | 未实现 |
| **Prime窗口** | 0-3m冷启动, 3-8m首批, 8-15m主力 | ❌ 无时间窗口 | 未实现 |
| **独立通道** | 与标准通道彻底隔离 | ❌ 同一pipeline | 未实现 |

**Roadmap（完整新币通道实现）**:
1. **Phase 1（当前v6.3.1）**: ✅ 判断标准符合规范（bars_1h + 14天阈值）
2. **Phase 2（Future）**: 获取真实 `since_listing` 时间（集成交易所API）
3. **Phase 3（Future）**: 实现48h渐变切换机制（需状态记录）
4. **Phase 4（Future）**: 构建独立新币pipeline:
   - 1m/5m/15m数据流（WS订阅）
   - 新币专用因子（ZLEMA_1m/5m）
   - 点火-成势-衰竭模型
   - AVWAP锚点计算
   - 更严执行闸门
   - Prime时间窗口限制

### 📊 修复效果

**v6.3（修复前）**:
- 新币阈值: ≤30天 ❌（规范要求14天）
- 判断依据: coin_age_hours（时间戳差）❌
- 数据受限: coin_age_hours >= 200 ⚠️

**v6.3.1（修复后）**:
- 新币阈值: bars_1h < 400 或 < 14天 ✅（符合规范）
- 判断依据: bars_1h（K线数量，主）+ coin_age_days（辅）✅
- 数据受限: bars_1h >= 200 ✅

---

## 📋 版本更新 (v6.2 → v6.3) - 2025-11-02

### 🎯 核心问题解决：0信号困境

**背景**: v6.2修复了4个Critical Bug后系统能正常运行，但扫描140个币种产生0个信号。专家分析指出系统过于严格，多层硬截断阈值叠加导致信号被过滤。

### 🔧 关键修复（专家建议采纳）

#### 修复 #1: 币龄分类数据受限处理
- **问题**: K线缓存限制（300根1h K线≈299小时）导致成熟币仍显示为"新币B(299小时)"
- **影响**: 成熟币错误触发新币模板的严格阈值（prob_min=0.63, dims_ok≥4）
- **修复**: 检测数据受限情况（coin_age_hours ≥ 200），强制分类为"成熟币"
  ```python
  # v6.3: 数据受限时默认成熟币
  data_limited = coin_age_hours >= 200
  if data_limited:
      coin_phase = "mature"  # 避免误判为新币
  ```
- **文件**:
  - `ats_core/pipeline/analyze_symbol.py:163-205`
  - `ats_core/pipeline/batch_scan_optimized.py:476-507`
- **备注**: 真实币龄查询需集成交易所API，当前为数据可用性的务实妥协

#### 修复 #2: F/I调制器软化处理
- **问题**: F/I因子在±100边界饱和，创建硬截断效应（F=-100时完全否决）
- **影响**: 资金费率极端时（如F=-100）直接阻断信号，无渐变过渡
- **修复**: 应用tanh()函数软化极值
  ```python
  # v6.3: 软化调制器，避免硬截断
  import math
  F = 100 * math.tanh(F_raw / 50)  # -100→-96, -50→-76
  I = 100 * math.tanh(I_raw / 50)  # 渐变而非硬切
  ```
- **文件**: `ats_core/pipeline/analyze_symbol.py:375-384, 349-356`
- **效果**: 将硬否决改为梯度惩罚，保留极端情况下的信号可能性

#### 修复 #3: EV门改为加分项
- **问题**: EV门当前为硬性要求 `max(0.0, (P-0.5)*2)`，P<0.5时EV=0.0成为硬截断
- **影响**: 概率稍低于0.5的信号（如P=0.49）EV直接归零，充当隐性否决门
- **修复**: 允许负值范围，改为加分/扣分制
  ```python
  # v6.3: EV改为可选加分项，允许负值
  "ev_gate": (P_chosen - 0.5) * 2,  # 范围-1到+1，非0-1
  ```
- **文件**: `ats_core/pipeline/analyze_symbol.py:749-760`
- **效果**: EV从必需门槛降为评分因素，不再单独否决信号

#### 修复 #4: 降低min_score默认阈值
- **问题**: 默认min_score=70，但测试中最高prime_strength仅18分，阈值与实际分布脱节
- **影响**: 即使是Prime信号也因min_score过滤而无法通过
- **修复**: 降低默认值从70到35
  ```python
  # v6.3: 降低阈值适配实际分布
  async def scan(self, min_score: int = 35, ...):
  ```
- **文件**: `ats_core/pipeline/batch_scan_optimized.py:405`
- **备注**: 阈值可根据实际运行数据进一步调整

#### 修复 #5: 拒绝原因跟踪（诊断工具）
- **问题**: 缺乏信号被拒绝的详细原因，难以诊断0信号问题
- **影响**: 无法定位是哪个门/阈值导致过滤
- **修复**: 添加rejection_reason字段记录拒绝详情
  ```python
  # v6.3: 跟踪拒绝原因
  rejection_reason = []
  if not is_prime:
      if prime_strength < 25:
          rejection_reason.append(f"Prime强度不足({prime_strength:.1f} < 25)")
          if base_strength < 15:
              rejection_reason.append(f"  - 基础强度过低({base_strength:.1f}/60)")
      # ... 详细原因
  ```
- **文件**:
  - `ats_core/pipeline/analyze_symbol.py:637-666, 761`
  - `ats_core/pipeline/batch_scan_optimized.py:598-612`
- **输出**: 日志显示前2条拒绝原因，便于分析瓶颈

### 📊 修复效果预期

**v6.2状态（修复前）**:
- 扫描140个币种：0个信号 ❌
- 典型问题：
  - F=-100（硬否决）, I=-53
  - EV=0.00（P<0.5时硬截断）
  - prime_strength最高18分 < min_score=70
  - 币龄299h触发新币严格阈值

**v6.3预期（修复后）**:
- 扫描140个币种：预期5-15个信号 ✅
- 改进点：
  - F/I软化：-100→-96（保留信号）
  - EV允许负值：不再单独否决
  - min_score=35：匹配实际分布
  - 成熟币正确分类：使用放宽阈值
  - 拒绝原因可见：便于持续优化

### 🔬 专家建议对比（采纳vs延期）

✅ **立即采纳**:
1. 币龄数据受限处理（务实妥协方案）
2. F/I调制器软化（高影响，低风险）
3. EV改为加分项（架构改进）
4. 降低min_score（快速验证）
5. 拒绝原因跟踪（诊断工具）

⏸️ **延期处理**（需更大范围改动）:
1. 交易所API集成查询真实上币时间（需外部依赖）
2. 阈值层解耦（需重构信号生成逻辑）
3. 性能优化（缓存/并行/向量化）（当前速度可接受）

---

## 📋 版本更新 (v6.1 → v6.2) - 2025-11-02

### 🐛 Critical Bug修复

#### Bug 1: I因子双重归一化
- **问题**: `calculate_independence()`已通过StandardizationChain返回±100分数，但analyze_symbol.py又做了一次0-100到-100的映射，导致I值可达-300到+100
- **影响**: 大量币种显示I≤-150，严重偏离±100标准化范围
- **修复**: 移除二次映射，直接使用`calculate_independence()`的返回值
- **文件**: `ats_core/pipeline/analyze_symbol.py:328-340`
- **参考**: 与L因子修复相同（同类型的双重归一化bug）
- **Hotfix (2025-11-02)**: 修复解包错误 - calculate_independence()返回3个值(score, beta_sum, meta)，但代码只解包了2个，导致"too many values to unpack (expected 2)"错误

#### Bug 2: 币龄计算错误
- **问题**: 使用K线数量(`len(k1h)`)作为币龄，导致缓存限制导致BTC/ETH等成熟币被误判为"新币B(300小时)"
- **影响**:
  - 成熟币触发新币模板的流动性惩罚
  - 错误的数据完整性要求
  - 分类显示错误
- **修复**: 使用K线时间戳差值计算真实币龄
  ```python
  coin_age_ms = latest_kline_ts - first_kline_ts
  coin_age_hours = coin_age_ms / (1000 * 3600)
  ```
- **文件**:
  - `ats_core/pipeline/analyze_symbol.py:150-161`
  - `ats_core/pipeline/batch_scan_optimized.py:465-474`

#### Bug 3: 四门系统缺失
- **问题**: batch_scan_optimized.py尝试读取`result.get('gates', {})`但analyze_symbol.py返回结果中没有'gates'键
- **影响**: 四门调节全部显示0.00，无法评估信号质量
- **修复**: 添加简化版四门系统到analyze_symbol返回结果
  - DataQual: 基于K线完整性
  - EV: 基于概率的简化估算
  - Execution: 基于流动性L因子
  - Probability: 基于P_chosen归一化
- **文件**: `ats_core/pipeline/analyze_symbol.py:731-748`
- **备注**: 当前为简化版，完整版需集成`integrated_gates.py`的FourGatesChecker

#### Bug 4: min_score参数未使用
- **问题**: batch_scan_optimized.py的scan()方法声明了min_score参数但未在过滤逻辑中使用
- **影响**: 无法通过min_score参数控制信号质量阈值
- **修复**: 添加过滤条件 `if is_prime and prime_strength >= min_score`
- **文件**: `ats_core/pipeline/batch_scan_optimized.py:585-588`

### 📊 修复效果预期

**修复前**:
- I因子: -300 到 +100（严重超出范围）
- 币龄: BTC/ETH显示为"新币B(300小时)"
- 四门: 全部显示0.00（失效）
- confidence: 5-20（极低，受I因子异常影响）
- prime_strength: base 3-12（无法达到阈值）

**修复后**:
- I因子: -100 到 +100（标准化范围）
- 币龄: 基于真实时间戳，BTC/ETH正确识别为成熟币
- 四门: 显示有意义的数值（0-1范围）
- confidence: 预期提升到正常范围（30-80）
- prime_strength: 预期提升，可能出现Prime信号

---

## 📋 版本更新 (v6.0 → v6.1)

### 🔧 关键修复

#### P0: I因子架构修正
- **问题**: I因子错误地放在A层参与评分，违反规范
- **修复**: 将I因子从A层移至B层（调制器层）
- **影响**:
  - A层从10因子减少到9因子
  - I的8%权重重新分配：T+2%, M+3%, S+4%, C+6%, L+4%, 其他rebalance
  - F和I权重设为0（不参与评分）

#### P1: 降低阈值以增加信号量
- **Prime强度阈值**: 35分 → 25分
- **概率阈值**: 0.62 → 0.58
- **维度达标数**: 4 → 3
- **维度阈值**: 35 → 30

#### P2: 放宽防抖动机制
- **入场阈值**: 0.80 → 0.65
- **确认K线**: 2/3 → 1/2（响应速度提升33%）
- **冷却时间**: 90秒 → 60秒

### 📊 预期效果

**修复前 (v6.0)**:
- 扫描140个币种：2-3个信号
- 通过四门：0-1个
- Prime信号：极少或无

**修复后 (v6.1)**:
- 扫描140个币种：8-15个信号
- 通过四门：5-10个
- Prime信号：3-7个/小时

---

## 🚀 快速部署（标准流程）

### 方式 1: 标准部署流程（推荐）

在服务器上执行以下命令：

```bash
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 标准部署流程（遵循 standards/DEPLOYMENT_STANDARD.md）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 第 1 步：拉取最新代码
cd ~/cryptosignal
git fetch origin claude/review-system-overview-011CUhLQjByWuXC1bySJCHKQ
git checkout claude/review-system-overview-011CUhLQjByWuXC1bySJCHKQ
git pull origin claude/review-system-overview-011CUhLQjByWuXC1bySJCHKQ

# 第 2 步：配置 Binance API（首次部署必需）
# 见下文 "Binance API 凭证配置"

# 第 3 步：运行部署脚本（自动验证 + 可选启动）
./deploy.sh

# 脚本会自动完成 8 步验证，最后询问是否立即启动（每5分钟扫描）
# 选择 y：自动启动生产环境 ⬅️ 推荐
# 选择 N：稍后手动启动
```

部署脚本会自动完成：
1. ✅ 停止旧进程
2. ✅ 备份配置文件
3. ✅ 拉取最新代码
4. ✅ 验证所有修复（权重、阈值、防抖动）
5. ✅ 清理Python缓存
6. ✅ 快速测试运行
7. ✅ 显示启动命令

### 方式 2: 快速启动（已部署完成后）

```bash
cd ~/cryptosignal
./start_production.sh
```

会提供三种启动方式供选择：
1. **Screen会话**（推荐）- 可分离后台运行
2. **nohup后台** - 传统后台运行
3. **前台运行** - 测试用

---

## 📝 手动部署步骤

如果需要手动执行，请按以下步骤操作：

### 第 1 步：停止旧进程

```bash
cd ~/cryptosignal

# 停止所有扫描器进程
ps aux | grep realtime_signal_scanner | grep -v grep | awk '{print $2}' | xargs kill 2>/dev/null

# 确认已停止
ps aux | grep realtime_signal_scanner | grep -v grep || echo "✅ 已停止"
```

### 第 2 步：备份配置

```bash
cd ~/cryptosignal

# 备份配置文件
BACKUP_TIME=$(date +%Y%m%d_%H%M%S)
cp config/params.json config/params.json.bak.$BACKUP_TIME
cp config/telegram.json config/telegram.json.bak.$BACKUP_TIME
cp config/binance_credentials.json config/binance_credentials.json.bak.$BACKUP_TIME

echo "✅ 配置已备份"
```

### 第 3 步：拉取v6.1代码

```bash
cd ~/cryptosignal

# 切换到v6.1分支
git fetch origin claude/review-system-overview-011CUhLQjByWuXC1bySJCHKQ
git checkout claude/review-system-overview-011CUhLQjByWuXC1bySJCHKQ
git pull origin claude/review-system-overview-011CUhLQjByWuXC1bySJCHKQ

# 查看最新提交
git log --oneline -5
```

应该看到以下提交：
- `669f403` - chore: 添加Binance凭证文件到.gitignore
- `55ba9d2` - fix: 修复系统审查发现的关键问题（合规度70%→90%+）

### 第 4 步：验证修复

```bash
cd ~/cryptosignal

# 验证权重配置
python3 -c "
import json
with open('config/params.json') as f:
    w = json.load(f)['weights']
    a_total = sum(w[k] for k in ['T','M','C','S','V','O','L','B','Q'])
    print(f'A层9因子总和: {a_total}%')
    print(f'F权重: {w[\"F\"]}, I权重: {w[\"I\"]}')
    assert a_total == 100.0 and w['F'] == 0 and w['I'] == 0
    print('✅ 权重配置正确')
"

# 验证Prime阈值
grep -n "is_prime = (prime_strength >=" ats_core/pipeline/analyze_symbol.py

# 验证防抖动参数
grep -A 5 "self.anti_jitter = AntiJitter" scripts/realtime_signal_scanner.py
```

### 第 5 步：清理缓存

```bash
cd ~/cryptosignal

# 清理Python缓存
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
find . -name "*.pyc" -delete 2>/dev/null

echo "✅ 缓存已清理"
```

### 第 6 步：测试运行

```bash
cd ~/cryptosignal

# 快速测试（10秒）
timeout 10 python3 scripts/realtime_signal_scanner.py --max-symbols 10 --no-telegram || echo "✅ 测试完成"
```

### 第 7 步：生产启动

#### 选项 A: Screen会话（推荐）

```bash
cd ~/cryptosignal

# 创建screen会话
screen -S cryptosignal

# 在screen中运行（完整扫描140币种 + Telegram通知）
python3 scripts/realtime_signal_scanner.py --interval 300

# 初始化完成后，按 Ctrl+A 然后 D 分离会话

# 重新连接
screen -r cryptosignal
```

#### 选项 B: 后台运行

```bash
cd ~/cryptosignal
mkdir -p logs

# 后台启动
nohup python3 scripts/realtime_signal_scanner.py --interval 300 > logs/scanner_$(date +%Y%m%d_%H%M%S).log 2>&1 &

# 查看PID
echo $!

# 查看日志
tail -f logs/scanner_*.log
```

#### 选项 C: 前台运行（测试用）

```bash
cd ~/cryptosignal

# 前台运行（Ctrl+C停止）
python3 scripts/realtime_signal_scanner.py --interval 300
```

---

## ✅ 验证清单

部署完成后，请确认以下项目：

### 配置文件验证
- [ ] `config/params.json` 存在且有效
- [ ] `config/binance_credentials.json` 存在且已填写真实API
- [ ] `config/telegram.json` 存在且已配置（可选）

### 代码修复验证
- [ ] A层9因子权重总和 = 100.0%
- [ ] F和I权重 = 0（B层调制器）
- [ ] Prime阈值 = 25分
- [ ] 概率阈值 = 0.58
- [ ] 防抖动阈值 = 0.65
- [ ] 确认K线 = 1/2
- [ ] 冷却时间 = 60秒

### 系统运行验证
- [ ] 系统成功初始化
- [ ] 没有导入错误
- [ ] 能够连接Binance API
- [ ] WebSocket连接正常
- [ ] 能够扫描币种并生成信号

### 监控验证（运行后）
- [ ] 查看日志输出正常
- [ ] Telegram通知能够发送
- [ ] 进程在后台稳定运行
- [ ] 每5分钟扫描一次（间隔300秒）

---

## 📊 监控与调优

### 实时监控

```bash
# 查看运行状态
ps aux | grep realtime_signal_scanner

# 查看日志（如果使用nohup）
tail -f logs/scanner_*.log

# 查看Screen会话
screen -ls
screen -r cryptosignal

# 统计信号数量（假设日志在logs目录）
grep -c "🔔 Prime信号" logs/scanner_*.log
```

### 24小时后统计

```bash
cd ~/cryptosignal

# 统计信号数量
echo "=== 信号统计 ==="
grep "🔔 Prime信号" logs/scanner_*.log | wc -l
grep "⚠️ Watch信号" logs/scanner_*.log | wc -l

# 统计四门通过率
echo "=== 四门统计 ==="
grep "DataQual门" logs/scanner_*.log | wc -l
grep "EV门" logs/scanner_*.log | wc -l
grep "Execution门" logs/scanner_*.log | wc -l
grep "Probability门" logs/scanner_*.log | wc -l
```

### 调优建议

如果运行24小时后：

**信号仍然太少**（< 2个Prime/小时）:
```json
// 进一步降低阈值 (config/params.json)
{
  "publish": {
    "prime_prob_min": 0.55,  // 0.58 → 0.55
    "prime_dims_ok_min": 2,  // 3 → 2
    "prime_dim_threshold": 25 // 30 → 25
  }
}
```

**假信号过多**（胜率 < 50%）:
```json
// 适当提高阈值 (config/params.json)
{
  "publish": {
    "prime_prob_min": 0.60,  // 0.58 → 0.60
    "prime_dims_ok_min": 4,  // 3 → 4
  }
}
```

**修改后重启**:
```bash
# 停止旧进程
ps aux | grep realtime_signal_scanner | grep -v grep | awk '{print $2}' | xargs kill

# 清理缓存
find ~/cryptosignal -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null

# 重新启动
cd ~/cryptosignal
./start_production.sh
```

---

## 🔧 故障排查

### 问题 1: 无法启动

**错误**: `ModuleNotFoundError: No module named 'aiohttp'`

**解决**:
```bash
cd ~/cryptosignal
pip3 install -r requirements.txt
```

### 问题 2: Binance API错误

**错误**: `FileNotFoundError: config/binance_credentials.json`

**解决**:
```bash
# 确保配置文件存在
ls -l config/binance_credentials.json

# 验证内容有效
python3 -c "
import json
with open('config/binance_credentials.json') as f:
    config = json.load(f)
    assert config['binance']['api_key'] != 'YOUR_BINANCE_API_KEY_HERE'
    print('✅ 配置有效')
"
```

### 问题 3: Telegram无法发送

**错误**: Telegram通知未收到

**解决**:
```bash
# 检查配置
cat config/telegram.json

# 测试Telegram连接
python3 -c "
import requests
import json

with open('config/telegram.json') as f:
    tg = json.load(f)

url = f\"https://api.telegram.org/bot{tg['bot_token']}/sendMessage\"
data = {
    'chat_id': tg['chat_id'],
    'text': '✅ CryptoSignal v6.1 测试消息'
}

resp = requests.post(url, json=data)
if resp.status_code == 200:
    print('✅ Telegram连接正常')
else:
    print(f'❌ 错误: {resp.text}')
"
```

### 问题 4: 进程异常退出

**现象**: 进程启动后自动退出

**排查步骤**:
```bash
# 1. 查看完整日志
tail -100 logs/scanner_*.log

# 2. 前台运行查看错误
cd ~/cryptosignal
python3 scripts/realtime_signal_scanner.py --interval 300

# 3. 检查权限
ls -l scripts/realtime_signal_scanner.py

# 4. 检查Python版本（需要3.8+）
python3 --version
```

---

## 📚 相关文档

- **修复方案**: `SYSTEM_FIX_PLAN.md`
- **系统概览**: `docs/SYSTEM_REVIEW_2025-11.md`
- **规范文档**:
  - `standards/CORE_STANDARDS.md` - 总规范
  - `standards/specifications/MODULATORS.md` - B层调制器规范
  - `standards/specifications/PUBLISHING.md` - 发布规范
- **快速参考**: `standards/QUICK_REFERENCE.md`

---

## 📞 支持

如遇到问题，请提供以下信息：

1. **Git版本**: `git log --oneline -3`
2. **配置验证**: 运行 `./deploy.sh` 的输出
3. **错误日志**: 最近50行日志
4. **系统信息**: Python版本、操作系统

---

**版本**: v6.1
**更新日期**: 2025-11-01
**分支**: `claude/review-system-overview-011CUhLQjByWuXC1bySJCHKQ`
**关键提交**: `55ba9d2`, `669f403`
