# CryptoSignal v6.7 完整工作流程

**创建时间**: 2025-11-06
**版本**: v6.7 P2.2
**文档类型**: 系统工作流程完整说明

---

## 📋 目录

1. [系统概览](#系统概览)
2. [启动流程](#启动流程)
3. [数据获取](#数据获取)
4. [因子计算](#因子计算)
5. [调制器系统](#调制器系统)
6. [评分与概率](#评分与概率)
7. [四门系统](#四门系统)
8. [防抖动机制](#防抖动机制)
9. [信号发布](#信号发布)
10. [完整流程图](#完整流程图)

---

## 系统概览

CryptoSignal v6.7 是一个加密货币信号分析系统，采用 **6因子+4调制器** 架构：

### 核心架构

```
A层（方向评分，权重100%）：
├─ T (Trend)      趋势因子     24%
├─ M (Momentum)   动量因子     10%  ← v6.7 P2.2降低（T-M相关66.4%）
├─ C (CVD Flow)   资金流因子   27%  ← v6.7 P2.2提升
├─ V (Volume)     量能因子     12%
├─ O (OI)         持仓量因子   21%  ← v6.7 P2.2提升
└─ B (Basis)      基差因子     6%

B层（调制执行参数，权重0%）：
├─ L (Liquidity)  流动性调制器 → 调节 position_mult, cost
├─ S (Structure)  结构调制器   → 调节 confidence, Teff
├─ F (Funding)    资金领先调制器 → 调节 Teff, p_min
└─ I (Independence) 独立性调制器 → 调节 Teff, cost
```

### 主入口文件

```bash
./setup.sh                                    # 一键部署入口
    └─> ./deploy_and_run.sh                  # 部署运行
        └─> scripts/realtime_signal_scanner.py  # 实时扫描器
            └─> ats_core/pipeline/analyze_symbol.py  # 核心分析
```

---

## 启动流程

### 1. 系统初始化 (setup.sh)

```bash
#!/bin/bash
# 执行流程：

1. 检测环境
   ├─ Python 3.8+
   ├─ pip3
   ├─ git
   └─ screen（可选）

2. 检查配置
   ├─ config/binance_credentials.json  # 必需
   ├─ config/telegram.json             # 可选
   └─ config/params.json               # 自动存在

3. 安装依赖
   └─ pip3 install -r requirements.txt

4. 启动系统
   └─ ./deploy_and_run.sh
```

### 2. 扫描器初始化 (realtime_signal_scanner.py)

```python
# 初始化阶段（3-4分钟）

class SignalScanner:
    async def initialize(self):
        """
        初始化WebSocket缓存

        步骤：
        1. 加载Telegram配置
        2. 初始化防抖动系统 (AntiJitter)
        3. 初始化批量扫描器 (OptimizedBatchScanner)
           ├─ 获取200个高流动性币种列表
           ├─ 批量初始化K线缓存（300根1h + 200根4h + 200根15m）
           ├─ 批量获取订单簿深度（20档）
           ├─ 批量获取持仓量历史（300根1h）
           └─ 初始化WebSocket连接（kline_1m/5m/15m实时流）

        耗时：3-4分钟（首次）
        """
```

**初始化数据结构：**

| 数据类型 | 数量 | 用途 | 更新方式 |
|---------|-----|------|---------|
| K线缓存 | 300根1h + 200根4h + 200根15m | 因子计算 | Layer 1/2智能触发 |
| 订单簿 | 20档深度 | L因子（流动性） | **仅初始化时获取** |
| 持仓量 | 300根1h历史 | O因子 | Layer 3定时更新 |
| 资金费率 | 当前值 | B因子 | Layer 3定时更新 |
| WebSocket | 实时K线流 | 价格更新 | 实时订阅 |

---

## 数据获取

### 三层数据更新机制

```python
# Layer 1: 价格快速更新（每次扫描，0.2秒）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 更新最后一根K线的收盘价/最高价/最低价
# 触发时机：每次扫描
# 数据来源：WebSocket实时流
# 影响因子：T/M/V（价格行为因子）

# Layer 2: 完整K线更新（智能触发，8-15秒）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 触发时机：
#   - 15m K线：02, 17, 32, 47分
#   - 1h/4h K线：05, 07分
# 获取内容：最新2根完整K线（包含takerBuyVolume等详细数据）
# 影响因子：C（CVD资金流计算更准确）

# Layer 3: 市场数据更新（定时，20-30秒）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 触发时机：00, 30分
# 更新内容：
#   - 资金费率（Funding Rate）
#   - 持仓量（Open Interest）
#   - 现货价格（Spot Price）
# 影响因子：O, B
```

### 数据新鲜度保证

| 时机 | CVD数据 | 订单簿 | 资金费率 | 持仓量 |
|------|--------|-------|---------|--------|
| **每次扫描** | ✅ 最新 | ⚠️ 缓存 | ⏸️ 定时 | ⏸️ 定时 |
| **Layer 2触发** | ✅ 更准确 | ⚠️ 缓存 | ⏸️ 定时 | ⏸️ 定时 |
| **Layer 3触发** | ✅ 最新 | ⚠️ 缓存 | ✅ 更新 | ✅ 更新 |
| **1小时后** | ✅ 实时 | ⚠️ 1小时前 | ✅ 有效 | ✅ 有效 |

---

## 因子计算

### A层：6个方向因子（参与评分）

#### 1. T因子（趋势）- 权重 24%

```python
def _calc_trend(h, l, c, c4, params) -> Tuple[float, dict]:
    """
    计算趋势强度：-100（强下跌）到 +100（强上涨）

    核心逻辑：
    1. EMA均线排序检查（EMA5 > EMA10 > EMA20）
    2. 价格斜率计算（基于ATR归一化）
    3. 综合趋势分数

    数据来源：
    - 1h K线（高/低/收盘价）
    - 4h K线（增强稳定性）

    输出范围：[-100, +100]
    """
    # 示例：
    # EMA5 = 106500, EMA10 = 106000, EMA20 = 105500
    # slope_atr = 2.5 (斜率2.5个ATR)
    # → T = +85 (强上涨趋势)
```

#### 2. M因子（动量）- 权重 10% (v6.7 P2.2降低)

```python
def _calc_momentum(h, l, c, params) -> Tuple[float, dict]:
    """
    计算动量：-100（减速下跌）到 +100（加速上涨）

    v6.7 P2.2优化：
    - 短窗口设计：EMA3/5（vs T因子的EMA5/20）
    - 目的：与T因子正交化，减少信息重叠
    - 降低权重：17% → 10%（T-M相关性66.4%）

    核心逻辑：
    1. 价格斜率（slope）：速度
    2. 价格加速度（accel）：变化率
    3. 加权组合：slope(60%) + accel(40%)

    输出范围：[-100, +100]
    """
    # 示例：
    # slope = +50 (上涨中)
    # accel = +30 (加速中)
    # → M = 0.6*50 + 0.4*30 = +42
```

#### 3. C因子（CVD资金流）- 权重 27% (v6.7 P2.2提升)

```python
def _calc_cvd_flow(cvd_series, c, params, klines) -> Tuple[float, dict]:
    """
    计算资金流：-100（流出）到 +100（流入）

    CVD计算：
    CVD = Σ(买入量 - 卖出量)
    delta = 2.0 * taker_buy - total_volume

    数据来源：
    - Layer 1: 从最新K线计算CVD（实时）
    - Layer 2: 从完整K线重新计算CVD（更准确）

    v6.7 P2.2增强：
    - 权重提升：24% → 27%
    - 强化资金流分析能力

    输出范围：[-100, +100]
    """
    # 示例：
    # 6h CVD变化 = +150 BTC
    # 归一化后 → C = +75 (资金流入)
```

#### 4. V因子（量能）- 权重 12%

```python
def _calc_volume(q, closes) -> Tuple[float, dict]:
    """
    计算量能：-100（缩量）到 +100（放量）

    核心逻辑：
    1. 当前3根vs 20根平均成交量对比
    2. 放量程度归一化

    v2.0修复：
    - 传入closes修复多空对称性
    - 放量上涨 → V = +100
    - 放量下跌 → V = -100 (考虑价格方向)

    输出范围：[-100, +100]
    """
```

#### 5. O因子（持仓量）- 权重 21% (v6.7 P2.2提升)

```python
def _calc_oi(symbol, c, params, cvd6, oi_data) -> Tuple[float, dict]:
    """
    计算持仓量：-100（减少）到 +100（增加）

    核心逻辑：
    1. OI 24h变化百分比
    2. OI与价格方向一致性
    3. 过热veto机制（避免追高）

    v6.7 P2.2增强：
    - 权重提升：17% → 21%
    - 强化资金流分析能力

    更新频率：Layer 3（每30分钟）

    输出范围：[-100, +100]
    """
    # 示例：
    # OI 24h变化 = +8%
    # 价格上涨 +2%
    # → O = +80 (OI增加 + 方向一致)
```

#### 6. B因子（基差）- 权重 6%

```python
def calculate_basis_funding(perp_price, spot_price, funding_rate, params):
    """
    计算基差：-100（看跌）到 +100（看涨）

    核心逻辑：
    1. 期现价差 = (期货价 - 现货价) / 现货价
    2. 资金费率方向
    3. 加权组合：basis(60%) + funding(40%)

    更新频率：
    - 现货价格：每次扫描
    - 资金费率：Layer 3（每30分钟）

    输出范围：[-100, +100]
    """
    # 示例：
    # 期货价 = 106500, 现货价 = 106000
    # 基差 = +0.47% (轻微溢价)
    # funding_rate = +0.01% (轻微多头费率)
    # → B = +25
```

---

### B层：4个调制器（不参与评分）

#### 1. L调制器（流动性）

```python
def calculate_liquidity(orderbook, params):
    """
    流动性调制器：调节 position_mult, cost

    核心逻辑：
    1. 价格带法：±40bps聚合订单簿深度
    2. 价差（spread）：买卖价差
    3. 冲击成本（impact）：50k USDT冲击
    4. OBI（订单簿不平衡）：买卖盘比例

    调制效果：
    - L = +100 → position_mult = 1.0 (满仓位)
    - L = 0    → position_mult = 0.65
    - L = -100 → position_mult = 0.30 (最小仓位)

    数据更新：⚠️ 仅初始化时获取，不实时更新

    输出范围：[-100, +100]
    """
```

#### 2. S调制器（结构）

```python
def _calc_structure(h, l, c, ema30, atr, params, ctx):
    """
    结构调制器：调节 confidence, Teff

    核心逻辑：
    1. 价格结构质量（支撑/阻力）
    2. 波动率适应
    3. 市场状态（bigcap/overlay/phaseA/strong）

    调制效果：
    - S = +100 → confidence *= 1.30 (提高30%)
    - S = 0    → confidence *= 1.0
    - S = -100 → confidence *= 0.70 (降低30%)

    输出范围：[-100, +100]
    """
```

#### 3. F调制器（资金领先）

```python
def _calc_fund_leading(oi_data, cvd_series, c, params):
    """
    资金领先调制器：调节 Teff, p_min

    核心逻辑：
    1. OI变化趋势
    2. CVD变化趋势
    3. 价格变化趋势
    4. 判断资金是否领先价格

    调制效果：
    - F = +100 → Teff *= 1.20 (提高响应)
    - F = 0    → Teff *= 1.0
    - F = -100 → Teff *= 0.80 (降低响应)

    特殊：过热veto机制
    - 基差 > P90 或 funding > P90 → 大幅惩罚

    输出范围：[-100, +100]
    """
```

#### 4. I调制器（独立性）

```python
def calculate_independence(symbol_prices, btc_prices, eth_prices, params):
    """
    独立性调制器：调节 Teff, cost

    核心逻辑：
    1. 计算与BTC的相关性
    2. 计算与ETH的相关性
    3. 独立性 = 1 - max(相关性)

    调制效果：
    - I = +100 (完全独立) → Teff *= 1.15, cost *= 0.85
    - I = 0   (中等相关) → Teff *= 1.0,  cost *= 1.0
    - I = -100 (完全相关) → Teff *= 0.85, cost *= 1.15

    输出范围：[-100, +100] (归一化)
    """
```

---

## 调制器系统

### 调制器链执行

```python
# ats_core/modulators/modulator_chain.py

class ModulatorChain:
    def modulate_all(self, L_score, S_score, F_score, I_score,
                     L_components, confidence_base, symbol):
        """
        统一调制器链：连续调节执行参数

        执行顺序：
        1. L调制器 → position_mult, cost
        2. S调制器 → confidence
        3. F调制器 → Teff, p_min
        4. I调制器 → Teff, cost

        输出：
        - position_mult_final  # 仓位倍数
        - cost_final          # 执行成本
        - confidence_final    # 置信度
        - Teff_final         # 温度（响应度）
        - p_min_final        # 概率门槛
        """

        # 示例调制过程：
        # 输入：
        #   L = +80 (高流动性)
        #   S = +60 (好结构)
        #   F = +90 (资金领先)
        #   I = +50 (中等独立)
        #   confidence_base = 0.75

        # L调制：
        position_mult = 0.30 + (L+100)/200 * 0.70  # = 0.93
        cost = 0.0015 * (1 - L/200)                 # = 0.00090

        # S调制：
        confidence = 0.75 * (0.70 + S/200 * 0.60)  # = 0.98

        # F调制：
        Teff = 2.0 * (0.80 + F/200 * 0.40)         # = 2.36
        p_min = 0.52 * (1 - F/400)                 # = 0.405

        # I调制：
        Teff *= (0.85 + I/200 * 0.30)              # = 2.95
        cost *= (1 - I/400)                        # = 0.00079

        # 输出：
        return ModulatorOutput(
            position_mult_final = 0.93,
            cost_final = 0.00079,
            confidence_final = 0.98,
            Teff_final = 2.95,
            p_min_final = 0.405
        )
```

---

## 评分与概率

### 1. 加权分数计算

```python
# ats_core/scoring/scorecard.py

def scorecard(scores, weights):
    """
    计算加权分数

    输入：
    - scores: {T: 85, M: 42, C: 75, V: 60, O: 80, B: 25}
    - weights: {T: 24, M: 10, C: 27, V: 12, O: 21, B: 6}

    计算过程：
    weighted_sum = 85*24 + 42*10 + 75*27 + 60*12 + 80*21 + 25*6
                 = 2040 + 420 + 2025 + 720 + 1680 + 150
                 = 7035

    weight_sum = 24 + 10 + 27 + 12 + 21 + 6 = 100

    weighted_score = 7035 / 100 = 70.35

    置信度计算：
    因子一致性 = (5个正因子 / 6个总因子) = 0.83
    confidence = 0.83 * 质量分数

    边际优势：
    edge = weighted_score / 100 = 0.7035

    输出：
    - weighted_score: 70.35
    - confidence: 0.75
    - edge: 0.7035
    """
```

### 2. 概率映射

```python
# ats_core/scoring/probability_v2.py

def map_probability_sigmoid(edge, prior_up, quality_score, temperature):
    """
    Sigmoid概率映射（世界顶级优化）

    公式：
    P = prior + quality * sigmoid(edge * temperature)

    示例计算：
    edge = 0.7035
    prior_up = 0.50
    quality_score = 0.95
    temperature = 2.95 (来自调制器)

    sigmoid_input = 0.7035 * 2.95 = 2.075
    sigmoid_output = 1 / (1 + e^(-2.075)) = 0.888

    P_long = 0.50 + 0.95 * 0.888 = 0.50 + 0.844 = 0.844
    P_short = 1 - P_long = 0.156

    输出：
    - P_long_base: 0.844
    - P_short_base: 0.156
    """
```

### 3. EV计算

```python
def calculate_EV(P_chosen, edge, cost_final):
    """
    期望值计算

    公式：
    EV = P * Edge - (1-P) * Cost

    示例：
    P_chosen = 0.844 (做多概率)
    edge = 0.7035 (边际优势)
    cost_final = 0.00079 (调制后成本)

    EV = 0.844 * 0.7035 - (1-0.844) * 0.00079
       = 0.594 - 0.000123
       = 0.594

    解释：
    - 每次交易预期收益 +59.4%
    - EV > 0 → 有利可图
    """
```

---

## 四门系统

### v6.6 软约束系统

```python
# ats_core/gates/integrated_gates.py

# v6.6架构变更：
# - 仅 DataQual < 0.90 为硬拒绝（硬门槛）
# - EV ≤ 0 和 P < p_min 为软约束（仅标记，不拒绝）

# Gate 1: DataQual（数据质量）- 唯一硬门槛
if data_qual < 0.90:
    return {
        "success": False,
        "error": "数据质量不足: DataQual < 0.90",
        "rejection_type": "hard_gate_dataqual"
    }

# Gate 2: EV（期望值）- 软约束
soft_filtered = False
if EV <= 0:
    soft_filtered = True
    soft_warnings.append(f"EV≤0 ({EV:.4f})")
    # 不硬拒绝，继续执行

# Gate 3: Execution（执行质量）- 软约束
# 基于L因子（流动性）
if L < -50:
    soft_warnings.append(f"流动性差 (L={L})")
    # 不硬拒绝，继续执行

# Gate 4: Probability（概率门槛）- 软约束
p_min = 0.68  # v6.7配置：提高阈值减少信号
if P_chosen < p_min:
    soft_filtered = True
    soft_warnings.append(f"P<p_min ({P_chosen:.3f} < {p_min})")
    # 不硬拒绝，继续执行

# 返回结果（软约束仅标记）
return {
    "success": True,
    "publish": {
        "soft_filtered": soft_filtered,  # True if any soft constraint failed
        "warnings": soft_warnings,        # 警告列表
        "EV": EV,
        "probability": P_chosen
    }
}
```

### 软约束效果

| 场景 | 硬约束时代(v6.5) | 软约束时代(v6.6) |
|------|----------------|----------------|
| **DataQual < 0.90** | ❌ 拒绝 | ❌ 拒绝（唯一硬门槛） |
| **EV ≤ 0** | ❌ 拒绝 | ✅ 通过，标记 `soft_filtered=True` |
| **P < p_min** | ❌ 拒绝 | ✅ 通过，标记 `soft_filtered=True` |
| **L < -50** | ❌ 拒绝 | ✅ 通过，标记警告 |

---

## 防抖动机制

### v6.6 K/N确认系统

```python
# ats_core/publishing/anti_jitter.py

class AntiJitter:
    """
    防抖动系统（v6.6: K/N=1/2确认）

    配置：
    - prime_entry_threshold: 0.45  # PRIME入场门槛
    - prime_maintain_threshold: 0.42  # PRIME维持门槛
    - watch_entry_threshold: 0.40  # WATCH入场门槛
    - confirmation_bars: 1  # 需要1根K线确认
    - total_bars: 2         # 总共看2根K线
    - cooldown_seconds: 60  # 冷却60秒
    """

    def update(self, symbol, probability, ev, gates_passed):
        """
        更新信号状态

        状态机：
        NONE → WATCH → PRIME → NONE

        转换条件：
        1. NONE → PRIME:
           - P ≥ 0.45
           - gates_passed = True
           - 连续1/2根K线满足
           - 冷却时间已过

        2. PRIME → NONE:
           - P < 0.42 (维持门槛)
           - 连续2根K线不满足

        3. NONE → WATCH:
           - 0.40 ≤ P < 0.45

        滞后性设计：
        - 入场门槛：0.45
        - 维持门槛：0.42（降低3%）
        - 目的：防止边界抖动
        """

        # 示例流程：
        # 第1次扫描：P=0.46 → state=NONE, bars_in_state=1 → should_publish=False
        # 第2次扫描：P=0.47 → state=PRIME, bars_in_state=2 → should_publish=True ✅
        # 第3次扫描：P=0.43 → state=PRIME (维持) → should_publish=True ✅
        # 第4次扫描：P=0.41 → state=NONE → should_publish=False

        return new_level, should_publish
```

### 防抖动效果

```
时间线示例（每5分钟扫描）：

23:02  P=0.46  EV=0.3   → state=NONE, 1/2确认  → ❌ 不发布
23:07  P=0.47  EV=0.4   → state=PRIME, 2/2确认 → ✅ 发布PRIME信号
23:12  P=0.43  EV=0.2   → state=PRIME (维持)   → ✅ 继续PRIME
23:17  P=0.41  EV=0.1   → state=NONE           → ❌ 降级NONE
23:22  P=0.44  EV=0.25  → state=NONE, 1/2确认  → ❌ 冷却中
23:27  P=0.46  EV=0.3   → state=NONE, 1/2确认  → ❌ 冷却未过
23:32  P=0.48  EV=0.4   → state=NONE, 冷却已过 → ❌ 等待1/2确认
23:37  P=0.49  EV=0.45  → state=PRIME, 2/2确认 → ✅ 再次发布
```

---

## 信号发布

### 1. Prime信号筛选

```python
# scripts/realtime_signal_scanner.py

async def scan_once(self):
    """
    单次扫描流程
    """

    # 1. 执行批量扫描
    scan_result = await self.scanner.scan(min_score=35)

    # 2. 提取所有信号
    signals = scan_result.get('results', [])

    # 3. Prime信号筛选
    prime_signals = []

    for s in signals:
        # 获取信号数据
        symbol = s.get('symbol')
        probability = s.get('probability')
        publish_info = s.get('publish', {})
        ev = publish_info.get('EV', 0.0)

        # v6.6: 软约束检查
        soft_filtered = publish_info.get('soft_filtered', False)

        # 所有通过analyze_symbol的信号都视为约束通过
        constraints_passed = True

        # 防抖动机制
        new_level, should_publish = self.anti_jitter.update(
            symbol=symbol,
            probability=probability,
            ev=ev,
            gates_passed=constraints_passed
        )

        # 发布条件（三重检查）：
        # 1. constraints_passed = True  (软约束通过)
        # 2. should_publish = True      (防抖动确认)
        # 3. new_level = 'PRIME'        (级别足够)

        if constraints_passed and should_publish and new_level == 'PRIME':
            # 添加软约束信息
            s['soft_constraints'] = {
                'passed': True,
                'warnings': [...],
                'ev': ev,
                'probability': probability
            }
            prime_signals.append(s)
            log(f"✅ {symbol}: PRIME信号确认")

    # 4. 发送到Telegram
    if prime_signals:
        await self._send_signals_to_telegram(prime_signals)
```

### 2. Telegram消息格式化

```python
# ats_core/outputs/telegram_fmt.py

def render_signal(signal, is_watch=False):
    """
    渲染Telegram消息（v6.7简洁版）

    消息结构：
    ━━━━━━━━━━━━━━━━━━━━━━━
    🚀 PRIME 信号
    ━━━━━━━━━━━━━━━━━━━━━━━

    📊 交易对: BTCUSDT
    📈 方向: 做多 LONG
    💰 入场: $106,500
    🎯 止盈1: $107,565 (+1.0%)
    🎯 止盈2: $109,162 (+2.5%)
    🛡️ 止损: $105,435 (-1.0%)

    📈 核心因子 (6因子系统)
    ━━━━━━━━━━━━━━━━━━━━━━━
    T 趋势    +85 ████████▌ 24%
    M 动量    +42 ████▏     10%
    C 资金流  +75 ███████▌  27%
    V 量能    +60 ██████    12%
    O 持仓    +80 ████████  21%
    B 基差    +25 ██▌        6%

    ⚙️ 执行调节 (4调制器)
    ━━━━━━━━━━━━━━━━━━━━━━━
    L 流动性  +80 → 仓位 93%
    S 结构    +60 → 信心 +30%
    F 资金先  +90 → 响应 +20%
    I 独立性  +50 → 成本 -12%

    📊 信号质量
    ━━━━━━━━━━━━━━━━━━━━━━━
    综合分数: 70.35
    概率: 84.4%
    期望值: +59.4%
    信心: 98%

    ⚠️ 软约束警告
    ━━━━━━━━━━━━━━━━━━━━━━━
    无警告 ✓

    ━━━━━━━━━━━━━━━━━━━━━━━
    ⏰ 2025-11-06 23:07 UTC
    v6.7 P2.2 | 6因子+4调制器
    """

    # 生成消息...
    return message
```

---

## 完整流程图

### 端到端流程

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 系统启动 (setup.sh → deploy_and_run.sh)                  │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. 扫描器初始化 (3-4分钟)                                    │
│   ├─ 加载配置 (params.json, telegram.json)                   │
│   ├─ 初始化防抖动系统 (AntiJitter)                           │
│   └─ 初始化批量扫描器 (OptimizedBatchScanner)                │
│       ├─ 获取200个币种列表                                   │
│       ├─ 批量缓存K线 (300×1h + 200×4h + 200×15m)           │
│       ├─ 批量获取订单簿 (20档深度)                          │
│       ├─ 批量获取持仓量 (300×1h历史)                        │
│       └─ 启动WebSocket实时订阅                              │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 定期扫描循环 (每5分钟)                                     │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
        ┌──────────────────────────┐
        │  对每个币种执行分析       │
        └──────────┬───────────────┘
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. 数据获取 & 更新                                            │
│   ├─ Layer 1: 更新最后K线价格 (0.2秒)                       │
│   ├─ Layer 2: 智能触发完整K线更新 (8-15秒)                  │
│   │    ├─ 15m: 02/17/32/47分触发                            │
│   │    └─ 1h/4h: 05/07分触发                                │
│   └─ Layer 3: 定时更新市场数据 (20-30秒)                    │
│        ├─ 00/30分触发                                        │
│        ├─ 资金费率                                           │
│        ├─ 持仓量                                             │
│        └─ 现货价格                                           │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 因子计算 (analyze_symbol_core)                            │
│                                                              │
│   5.1 A层方向因子 (6个)                                      │
│   ├─ T (趋势)      [-100, +100]  权重24%                    │
│   ├─ M (动量)      [-100, +100]  权重10% ← v6.7 P2.2降低   │
│   ├─ C (CVD资金流) [-100, +100]  权重27% ← v6.7 P2.2提升   │
│   ├─ V (量能)      [-100, +100]  权重12%                    │
│   ├─ O (持仓)      [-100, +100]  权重21% ← v6.7 P2.2提升   │
│   └─ B (基差)      [-100, +100]  权重6%                     │
│                                                              │
│   5.2 B层调制器 (4个) - 权重0%                              │
│   ├─ L (流动性)    [-100, +100] → position_mult, cost      │
│   ├─ S (结构)      [-100, +100] → confidence, Teff         │
│   ├─ F (资金领先)  [-100, +100] → Teff, p_min              │
│   └─ I (独立性)    [-100, +100] → Teff, cost               │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. 加权评分 (scorecard)                                      │
│   ├─ weighted_score = Σ(因子 × 权重) / 100                  │
│   ├─ confidence = 因子一致性 × 质量分数                      │
│   └─ edge = weighted_score / 100                            │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. 调制器链执行 (ModulatorChain)                             │
│   ├─ L调制 → position_mult, cost                            │
│   ├─ S调制 → confidence                                     │
│   ├─ F调制 → Teff, p_min                                    │
│   ├─ I调制 → Teff, cost                                     │
│   └─ 输出调制后的执行参数                                    │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 8. 概率计算 (map_probability_sigmoid)                        │
│   ├─ 使用调制后的 Teff                                      │
│   ├─ P = prior + quality × sigmoid(edge × Teff)             │
│   └─ 输出 P_long, P_short                                   │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 9. EV计算                                                    │
│   ├─ 使用调制后的 cost_final                                │
│   ├─ EV = P × edge - (1-P) × cost                           │
│   └─ 输出 EV                                                 │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 10. 四门系统 (软约束)                                        │
│   ├─ Gate 1: DataQual < 0.90 → ❌ 硬拒绝                   │
│   ├─ Gate 2: EV ≤ 0 → ✅ 软约束，仅标记                    │
│   ├─ Gate 3: L < -50 → ✅ 软约束，仅标记                   │
│   └─ Gate 4: P < p_min → ✅ 软约束，仅标记                 │
│                                                              │
│   输出: {success: True, publish: {soft_filtered, warnings}} │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 11. 防抖动机制 (AntiJitter)                                  │
│   ├─ 检查概率: P ≥ 0.45 (入场) / P ≥ 0.42 (维持)           │
│   ├─ K/N确认: 1/2根K线确认                                  │
│   ├─ 冷却检查: 距上次发布 ≥ 60秒                            │
│   └─ 输出: (new_level, should_publish)                      │
│                                                              │
│   状态转换:                                                  │
│   NONE → WATCH → PRIME → NONE                              │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 12. Prime信号筛选                                            │
│   三重检查:                                                  │
│   ├─ constraints_passed = True (软约束通过)                 │
│   ├─ should_publish = True (防抖动确认)                     │
│   └─ new_level = 'PRIME' (级别足够)                         │
│                                                              │
│   全部满足 → 添加到 prime_signals 列表                      │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│ 13. Telegram消息格式化 & 发送                                │
│   ├─ render_signal() 生成富媒体消息                         │
│   │   ├─ 交易对信息                                         │
│   │   ├─ 入场/止盈/止损价格                                 │
│   │   ├─ 6因子评分可视化                                    │
│   │   ├─ 4调制器效果                                        │
│   │   ├─ 信号质量指标                                       │
│   │   └─ 软约束警告                                         │
│   └─ telegram_send_wrapper() 发送到Telegram                 │
└─────────────────────────────────────────────────────────────┘
                       ↓
                  [循环继续]
```

---

## 性能指标

### 时间消耗统计

| 阶段 | 耗时 | 说明 |
|------|------|------|
| **初始化** | 3-4分钟 | 首次启动，批量缓存数据 |
| **Layer 1更新** | 0.2秒 | 每次扫描，更新价格 |
| **Layer 2更新** | 8-15秒 | 智能触发，完整K线 |
| **Layer 3更新** | 20-30秒 | 定时触发，市场数据 |
| **因子计算** | 0.5-1秒 | 单币种，6因子+4调制器 |
| **批量扫描** | 12-15秒 | 200个币种，完整分析 |

### API调用优化

| 操作 | v6.5 (每次) | v6.7 (每次) | 优化 |
|------|------------|------------|------|
| **K线获取** | 200次 | 0次 | WebSocket缓存 |
| **订单簿获取** | 200次 | 0次 | 初始化缓存 |
| **持仓量获取** | 200次 | 0次 (30分钟1次) | Layer 3定时 |
| **现货价格** | 200次 | 1次 | 批量获取 |
| **总API调用** | 800次/扫描 | 1次/扫描 | **↓99.9%** |

---

## 配置文件影响

### config/params.json 关键配置

```json
{
  "weights": {
    "T": 24.0,   // 趋势权重
    "M": 10.0,   // 动量权重 (v6.7 P2.2降低)
    "C": 27.0,   // 资金流权重 (v6.7 P2.2提升)
    "V": 12.0,   // 量能权重
    "O": 21.0,   // 持仓权重 (v6.7 P2.2提升)
    "B": 6.0,    // 基差权重
    "L": 0.0,    // 流动性调制器
    "S": 0.0,    // 结构调制器
    "F": 0.0,    // 资金领先调制器
    "I": 0.0     // 独立性调制器
  },

  "publish": {
    "prime_prob_min": 0.68,      // PRIME概率门槛 (v6.7提高)
    "prime_dims_ok_min": 3,      // PRIME因子一致性门槛
    "prime_dim_threshold": 30,   // 单因子门槛
    "watch_prob_min": 0.52,      // WATCH概率门槛
    "watch_prob_max": 0.67       // WATCH上限
  }
}
```

---

## 总结

完整流程从 **数据获取** → **因子计算** → **调制器调节** → **概率计算** → **四门检查** → **防抖动确认** → **Telegram发布**，形成了一个严密的信号生成和质量控制体系：

1. **三层数据更新**：保证数据新鲜度的同时优化性能
2. **6因子方向评分**：全面评估市场多空方向
3. **4调制器参数调节**：根据市场微观结构调整执行策略
4. **软约束系统**：平衡信号质量和数量
5. **防抖动机制**：避免虚假信号和频繁切换
6. **富媒体展示**：清晰传达信号质量和风险

整个系统每5分钟扫描200个币种，从数据到发布仅需12-15秒，实现了**高效、准确、稳定**的信号生成。

---

**文档版本**: v1.0
**维护者**: Claude AI
**最后更新**: 2025-11-06
