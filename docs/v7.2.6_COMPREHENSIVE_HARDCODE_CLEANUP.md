# v7.2.6 综合硬编码清理 - 新币阈值配置化

**修复日期**: 2025-11-10
**优先级**: P1 (High)
**总耗时**: ~1.5小时
**版本**: v7.2.6

---

## 🔍 问题概览

### 背景
在v7.2.5修复p0概率阈值硬编码后，进行全面系统性检查，发现新币相关阈值存在大量硬编码问题。

### 严重程度
**P1 - 高优先级问题**：新币相关的4个关键阈值被硬编码，导致：
- 配置文件中的新币相关阈值被完全忽略
- 无法通过配置调优新币策略
- 维护困难，修改阈值需要改代码

---

## 📊 发现的硬编码问题

### 问题1: watch_prob_min 完全硬编码（4处）

**ats_core/pipeline/analyze_symbol.py（修复前）**:
```python
if is_ultra_new:
    watch_prob_min = 0.65  # 硬编码！
elif is_phaseA:
    watch_prob_min = 0.60  # 硬编码！
elif is_phaseB:
    watch_prob_min = 0.60  # 硬编码！
else:
    watch_prob_min = publish_cfg.get("watch_prob_min", 0.65)
```

**配置文件状态**:
- `config/params.json` 中成熟币有 `watch_prob_min: 0.52`
- 但新币的三个阶段完全没有配置，全部硬编码在代码中

### 问题2: ultra_new的prime_dim_threshold硬编码

**ats_core/pipeline/analyze_symbol.py（修复前）**:
```python
if is_ultra_new:
    prime_dim_threshold = 70  # 硬编码！
elif is_phaseA:
    prime_dim_threshold = publish_cfg.get("prime_dim_threshold", 65)  # 从配置读取
elif is_phaseB:
    prime_dim_threshold = publish_cfg.get("prime_dim_threshold", 65)  # 从配置读取
```

**不一致性**:
- ultra_new是硬编码70
- 其他两个阶段从配置读取65
- 导致维护困难，配置不完整

---

## 🔧 修复方案

### 修复原则
遵循 **SYSTEM_ENHANCEMENT_STANDARD.md § 5: 统一配置管理**
- 所有阈值从配置文件读取
- 禁止Magic Number硬编码
- 提供合理默认值作为后备

### 修复文件清单

| 文件 | 变更类型 | 行数变更 | 说明 |
|------|---------|---------|------|
| `config/params.json` | 新增配置 | +4行 | 添加新币watch_prob_min和prime_dim_threshold |
| `ats_core/pipeline/analyze_symbol.py` | 移除硬编码 | 修改4行 | 所有新币阈值改为从配置读取 |
| `docs/v7.2.6_COMPREHENSIVE_HARDCODE_CLEANUP.md` | 新增文档 | 全新 | 本文档 |

---

## 📝 详细修复内容

### 1. 配置文件修复（config/params.json）

**添加的配置项**:
```json
"new_coin": {
  // ... 原有配置 ...
  "ultra_new_prime_dim_threshold": 70,      // 新增
  "ultra_new_watch_prob_min": 0.65,         // 新增
  "phaseA_watch_prob_min": 0.60,            // 新增
  "phaseB_watch_prob_min": 0.60,            // 新增
  "_comment": "v7.2.6修复：添加watch_prob_min和prime_dim_threshold配置，移除硬编码"
}
```

**完整的新币配置结构**（修复后）:
```json
"new_coin": {
  "enabled": true,
  "min_hours": 1,
  "ultra_new_hours": 24,
  "phaseA_days": 7,
  "phaseB_days": 30,
  "max_days": 30,
  "min_volume_24h": 10000000,

  // Ultra New（1-24小时）
  "ultra_new_prime_prob_min": 0.75,
  "ultra_new_dims_ok_min": 6,
  "ultra_new_prime_strength_min": 45,
  "ultra_new_prime_dim_threshold": 70,     // v7.2.6新增
  "ultra_new_watch_prob_min": 0.65,        // v7.2.6新增

  // Phase A（1-7天）
  "phaseA_prime_prob_min": 0.72,
  "phaseA_dims_ok_min": 5,
  "phaseA_prime_strength_min": 40,
  "phaseA_watch_prob_min": 0.60,           // v7.2.6新增

  // Phase B（7-30天）
  "phaseB_prime_prob_min": 0.70,
  "phaseB_dims_ok_min": 4,
  "phaseB_prime_strength_min": 35,
  "phaseB_watch_prob_min": 0.60            // v7.2.6新增
}
```

### 2. Pipeline代码修复（ats_core/pipeline/analyze_symbol.py）

**修改位置**: Lines 710-736

**修复前**:
```python
# 新币特殊处理：应用分级标准
if is_ultra_new:
    # 超新币（1-24小时）：超级谨慎
    prime_prob_min = new_coin_cfg.get("ultra_new_prime_prob_min", 0.70)
    prime_dims_ok_min = new_coin_cfg.get("ultra_new_dims_ok_min", 6)
    prime_dim_threshold = 70  # 硬编码！
    watch_prob_min = 0.65  # 硬编码！
elif is_phaseA:
    # 阶段A（1-7天）：极度谨慎
    prime_prob_min = new_coin_cfg.get("phaseA_prime_prob_min", 0.65)
    prime_dims_ok_min = new_coin_cfg.get("phaseA_dims_ok_min", 5)
    prime_dim_threshold = publish_cfg.get("prime_dim_threshold", 65)
    watch_prob_min = 0.60  # 硬编码！
elif is_phaseB:
    # 阶段B（7-30天）：谨慎
    prime_prob_min = new_coin_cfg.get("phaseB_prime_prob_min", 0.63)
    prime_dims_ok_min = new_coin_cfg.get("phaseB_dims_ok_min", 4)
    prime_dim_threshold = publish_cfg.get("prime_dim_threshold", 65)
    watch_prob_min = 0.60  # 硬编码！
```

**修复后**:
```python
# 新币特殊处理：应用分级标准
# v7.2.6修复：移除所有硬编码，从配置文件读取
if is_ultra_new:
    # 超新币（1-24小时）：超级谨慎
    prime_prob_min = new_coin_cfg.get("ultra_new_prime_prob_min", 0.70)
    prime_dims_ok_min = new_coin_cfg.get("ultra_new_dims_ok_min", 6)
    prime_dim_threshold = new_coin_cfg.get("ultra_new_prime_dim_threshold", 70)  # v7.2.6修复
    watch_prob_min = new_coin_cfg.get("ultra_new_watch_prob_min", 0.65)  # v7.2.6修复
elif is_phaseA:
    # 阶段A（1-7天）：极度谨慎
    prime_prob_min = new_coin_cfg.get("phaseA_prime_prob_min", 0.65)
    prime_dims_ok_min = new_coin_cfg.get("phaseA_dims_ok_min", 5)
    prime_dim_threshold = publish_cfg.get("prime_dim_threshold", 65)
    watch_prob_min = new_coin_cfg.get("phaseA_watch_prob_min", 0.60)  # v7.2.6修复
elif is_phaseB:
    # 阶段B（7-30天）：谨慎
    prime_prob_min = new_coin_cfg.get("phaseB_prime_prob_min", 0.63)
    prime_dims_ok_min = new_coin_cfg.get("phaseB_dims_ok_min", 4)
    prime_dim_threshold = publish_cfg.get("prime_dim_threshold", 65)
    watch_prob_min = new_coin_cfg.get("phaseB_watch_prob_min", 0.60)  # v7.2.6修复
```

**关键改进**:
1. ✅ 4个硬编码全部改为`config.get()`调用
2. ✅ 保留合理的默认值（与原硬编码值一致）
3. ✅ 添加v7.2.6修复标记，便于追溯
4. ✅ 保持代码逻辑不变，仅修改数据来源

---

## ✅ 测试验证

### Test 1: JSON格式验证
```bash
$ python3 -c "import json; json.load(open('config/params.json'))"
✅ params.json格式验证通过
```

### Test 2: 配置加载验证（关键测试）
```bash
$ python3 -c "
from ats_core.cfg import CFG
params = CFG.params
new_coin = params.get('new_coin', {})
print(f'ultra_new_watch_prob_min = {new_coin.get(\"ultra_new_watch_prob_min\")}')
print(f'phaseA_watch_prob_min = {new_coin.get(\"phaseA_watch_prob_min\")}')
print(f'phaseB_watch_prob_min = {new_coin.get(\"phaseB_watch_prob_min\")}')
print(f'ultra_new_prime_dim_threshold = {new_coin.get(\"ultra_new_prime_dim_threshold\")}')
"
✅ 配置加载成功
   ultra_new_watch_prob_min = 0.65
   phaseA_watch_prob_min = 0.6
   phaseB_watch_prob_min = 0.6
   ultra_new_prime_dim_threshold = 70
```

### Test 3: 模块导入验证
```bash
$ python3 -c "
import ats_core.pipeline.analyze_symbol
import ats_core.outputs.telegram_fmt
print('✅ 所有关键模块导入成功')
"
✅ 所有关键模块导入成功
```

**所有测试通过** ✓

---

## 📈 修复前后对比

### Before (v7.2.5)
```python
# 配置文件
"new_coin": {
  "ultra_new_prime_prob_min": 0.75,
  // 缺少 watch_prob_min
  // 缺少 prime_dim_threshold
}

# 代码
if is_ultra_new:
    watch_prob_min = 0.65  # 硬编码
    prime_dim_threshold = 70  # 硬编码
```

**问题**:
- ❌ 配置不完整
- ❌ 无法通过配置调优新币策略
- ❌ 修改阈值需要改代码，容易遗漏

### After (v7.2.6)
```python
# 配置文件
"new_coin": {
  "ultra_new_prime_prob_min": 0.75,
  "ultra_new_watch_prob_min": 0.65,         // 新增
  "ultra_new_prime_dim_threshold": 70,      // 新增
  "phaseA_watch_prob_min": 0.60,            // 新增
  "phaseB_watch_prob_min": 0.60             // 新增
}

# 代码
if is_ultra_new:
    watch_prob_min = new_coin_cfg.get("ultra_new_watch_prob_min", 0.65)
    prime_dim_threshold = new_coin_cfg.get("ultra_new_prime_dim_threshold", 70)
```

**改进**:
- ✅ 配置完整统一
- ✅ 可通过配置调优，无需改代码
- ✅ 所有阈值集中管理
- ✅ 符合§5统一配置管理规范

---

## 🎓 经验教训

### 1. 系统性检查的重要性
**问题**: 之前的修复（v7.2.3-v7.2.5）逐个发现硬编码问题，没有系统性扫描。

**教训**:
- 发现一个硬编码问题时，应立即全面扫描同类问题
- 使用grep系统性搜索所有阈值相关代码
- 建立硬编码检测清单

### 2. 配置完整性原则
**问题**: 代码使用`config.get()`但配置文件中缺少对应项。

**教训**:
- 配置文件应包含所有`config.get()`使用的键
- 避免"半配置化"（部分从配置读取，部分硬编码）
- 配置文件应与代码同步维护

### 3. 新币策略的特殊性
**问题**: 新币有3个阶段（ultra_new, phaseA, phaseB），每个阶段都有独立阈值。

**教训**:
- 复杂分级策略需要完整的配置支持
- 每个分支都应检查硬编码
- 测试时应覆盖所有分支逻辑

### 4. 渐进式修复的风险
**问题**: v7.2.3→v7.2.4→v7.2.5→v7.2.6逐步修复，每次只解决发现的问题。

**教训**:
- 第一次修复时应该做全面扫描
- 渐进式修复虽然安全，但可能遗漏问题
- 建立硬编码检测的自动化工具

---

## 🔍 系统性检查方法

本次修复使用的系统性检查流程：

### Step 1: 从启动脚本追踪
```bash
# 从setup.sh开始，追踪整个启动流程
./setup.sh → scripts/realtime_signal_scanner.py → analyze_symbol.py
```

### Step 2: 扫描所有数字常量
```bash
# 搜索所有可能的阈值硬编码
grep -rn "= 0\.(70|68|65|63|60)" ats_core/pipeline/
grep -rn "prime_prob_min\|watch_prob_min" ats_core/
```

### Step 3: 对比配置文件
```bash
# 检查代码使用的键是否在配置中存在
grep "new_coin_cfg.get(" analyze_symbol.py
# 对比 config/params.json 中的 new_coin 节点
```

### Step 4: 验证所有分支
```python
# 检查if-elif-else每个分支
if is_ultra_new:     # 检查这个分支
elif is_phaseA:      # 检查这个分支
elif is_phaseB:      # 检查这个分支
else:                # 检查这个分支
```

---

## 📌 修复覆盖范围

### 已修复的硬编码（v7.2.3-v7.2.6全系列）

| 版本 | 修复内容 | 数量 |
|------|---------|------|
| v7.2.3 | confidence_threshold, prime_strength_threshold | 2个 |
| v7.2.4 | edge_threshold, gate_multiplier, base_strength | 3个 |
| v7.2.5 | FIModulator p0概率阈值 | 1个 |
| **v7.2.6** | **新币watch_prob_min, prime_dim_threshold** | **4个** |
| **总计** | **10个硬编码阈值全部配置化** | **10个** |

### 剩余检查项（建议后续）

1. ⚠️  **其他模块中的硬编码**
   - `ats_core/features/*.py` 中的因子参数
   - `ats_core/modulators/*.py` 中的调制器参数
   - `ats_core/gates/*.py` 中的闸门参数

2. ⚠️  **默认值一致性**
   - 代码中的默认值应与配置文件中的值保持一致
   - 目前mature的prime_prob_min默认0.68，但signal_thresholds.json中是0.45

3. ⚠️  **配置文件版本统一**
   - params.json 和 signal_thresholds.json 两个配置文件存在部分重复
   - 建议统一到一个配置文件或明确分工

---

## 🚀 预期效果

### 直接效果
- ✅ 新币策略完全可配置，无需改代码
- ✅ 维护更简单，所有阈值集中管理
- ✅ 符合系统增强标准§5规范

### 间接效果
- ✅ 可以通过A/B测试优化新币策略
- ✅ 不同市场环境可以快速调整阈值
- ✅ 降低代码变更风险

### 无性能影响
- ✅ 配置加载一次，不影响运行时性能
- ✅ 保留默认值，配置缺失时仍可正常运行
- ✅ 代码逻辑完全不变

---

## 📚 相关文档

- **系统增强标准**: `standards/SYSTEM_ENHANCEMENT_STANDARD.md § 5`
- **前序修复文档**:
  - `docs/v7.2.3_P0_FIXES_SUMMARY.md`
  - `docs/v7.2.4_SYSTEMATIC_FIX.md`
  - `docs/v7.2.5_P0_PROBABILITY_THRESHOLD_FIX.md`
- **新币规范**: `standards/NEWCOIN_SPEC.md`

---

## ✅ 符合规范

本次修复完全符合 **SYSTEM_ENHANCEMENT_STANDARD.md**：

- ✅ § 5.1: 所有因子参数从配置文件读取
- ✅ § 5.1: 禁止Magic Number
- ✅ § 5.1: 提供默认值作为兜底
- ✅ § 3: 文档与代码同步
- ✅ 文件修改顺序：config → pipeline → docs

---

**当前版本**: v7.2.6
**最后更新**: 2025-11-10
**状态**: ✅ 修复完成，已测试验证
**影响范围**: 新币策略阈值管理
**向后兼容**: 完全兼容（保留默认值）
