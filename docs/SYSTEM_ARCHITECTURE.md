# CryptoSignal v7.2 系统架构与依赖关系图

生成时间：2025-11-14

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🏗️ 系统分层架构
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
┌─────────────────────────────────────────────────────────────┐
│                    🚀 应用入口层                              │
│  scripts/realtime_signal_scanner.py (主入口)                 │
│  └─ 实时扫描所有交易对，检测交易信号                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    📋 批量处理层                              │
│  ats_core/pipeline/batch_scan_optimized.py                  │
│  └─ OptimizedBatchScanner: 优化的批量扫描器                 │
│     ├─ 并发处理多个交易对                                    │
│     ├─ 内存优化和速度优化                                    │
│     └─ 错误处理和重试机制                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    🔬 核心分析层                              │
│  ats_core/pipeline/analyze_symbol.py (主分析流程)           │
│  ats_core/pipeline/analyze_symbol_v72.py (v7.2增强)         │
│  └─ analyze_symbol(symbol) → result                         │
│     ├─ 获取市场数据                                          │
│     ├─ 计算10个因子（T/M/C/V/O/B/F/L/S/I）                  │
│     ├─ 7道闸门过滤                                           │
│     ├─ v7.2增强（因子分组、统计校准）                       │
│     └─ 生成最终信号                                          │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────┼───────────────────┐
        ↓                   ↓                   ↓
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ 📊 数据源层  │    │ 🧮 因子计算层│    │ ⚙️ 配置层    │
└─────────────┘    └─────────────┘    └─────────────┘
        ↓                   ↓                   ↓
┌─────────────────────────────────────────────────────────────┐
│ Sources (数据获取)                                           │
│ - binance.py: 获取K线、价格、交易量等                        │
│ - klines.py: K线数据处理                                     │
│ - oi.py: 持仓量(OI)数据                                      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Features (因子计算) - 10个核心因子                            │
│ - trend.py: T因子（趋势）                                     │
│ - momentum.py: M因子（动量）                                  │
│ - confluence.py: C因子（多周期汇聚）                          │
│ - volume.py: V因子（成交量）                                  │
│ - oi_analysis.py: O因子（持仓量）                             │
│ - liquidity.py: L因子（流动性）                               │
│ - social.py: S因子（社交热度）                                │
│ - innovation.py: I因子（创新性/新币溢价）                     │
│                                                              │
│ Factors_v2 (v7.2新因子)                                      │
│ - funding_v2.py: F因子v2（资金费率精确定义）                 │
│ - balance.py: B因子（多空平衡）                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Config (配置管理)                                            │
│ - cfg.py: 全局配置入口                                        │
│ - threshold_config.py: 阈值配置（从JSON读取）                │
│ - factor_config.py: 因子权重配置                              │
│ - anti_jitter_config.py: 防抖动配置                           │
│                                                              │
│ Config Files (JSON配置)                                      │
│ - config/signal_thresholds.json: 信号阈值                    │
│ - config/factor_weights.json: 因子权重                       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    🎯 评分与过滤层                            │
│  Scoring (评分系统)                                          │
│  - scoring_utils.py: StandardizationChain（标准化）         │
│  - scoring_v2.py: v7.2评分逻辑                               │
│                                                              │
│  Calibration (统计校准)                                      │
│  - empirical_calibration.py: 经验校准器                      │
│                                                              │
│  Modulators (调节器)                                         │
│  - confidence_modulator.py: 信心度调节                       │
│  - new_coin_modulator.py: 新币调节                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    🔍 7道闸门过滤                             │
│  Gate1: 趋势强度过滤                                         │
│  Gate2: 信号方向一致性                                        │
│  Gate3: 动量确认                                             │
│  Gate4: 新币风险控制                                         │
│  Gate5: 因子分组质量（TC/VOM/B）                            │
│  Gate6: 统计校准（历史准确率）                               │
│  Gate7: 综合信心度阈值                                        │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    📤 输出与发布层                            │
│  Publishing (防抖动)                                         │
│  - anti_jitter.py: AntiJitter 防抖动逻辑                    │
│     ├─ 防止频繁发信号                                        │
│     ├─ 同一交易对冷却期                                      │
│     └─ 全局速率限制                                          │
│                                                              │
│  Outputs (格式化)                                            │
│  - telegram_fmt.py: 格式化为Telegram消息                    │
│     ├─ render_trade_v72(): 渲染信号详情                     │
│     ├─ 显示10因子得分                                        │
│     ├─ 显示7道闸门状态                                       │
│     └─ 显示置信度和建议                                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    💾 数据持久层                              │
│  Data (数据存储)                                             │
│  - analysis_db.py: SQLite数据库（分析结果）                  │
│  - trade_recorder.py: 交易记录器                             │
│                                                              │
│  Analysis (分析报告)                                         │
│  - report_writer.py: 生成扫描报告                            │
│  - scan_statistics.py: 扫描统计                              │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    🎬 执行层（未来扩展）                      │
│  Execution (交易执行)                                        │
│  - paper_trader.py: 模拟交易                                 │
│  - trade_executor.py: 真实交易执行器（占位）                │
└─────────────────────────────────────────────────────────────┘
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🔗 核心文件依赖关系
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 主入口 → 批量扫描

```
scripts/realtime_signal_scanner.py
    ↓ import
ats_core.pipeline.batch_scan_optimized.OptimizedBatchScanner
    ↓ import
ats_core.pipeline.analyze_symbol.analyze_symbol
```

### 单币分析流程

```
ats_core.pipeline.analyze_symbol.analyze_symbol(symbol)
    │
    ├─→ ats_core.sources.binance.get_klines()           # 获取K线
    ├─→ ats_core.sources.oi.fetch_oi_history()          # 获取持仓量
    │
    ├─→ ats_core.features.trend.analyze_trend()         # T因子
    ├─→ ats_core.features.momentum.calc_momentum()      # M因子
    ├─→ ats_core.features.confluence.check_confluence() # C因子
    ├─→ ats_core.features.volume.calc_volume_score()    # V因子
    ├─→ ats_core.features.oi_analysis.calc_oi_score()   # O因子
    ├─→ ats_core.features.liquidity.calc_liquidity()    # L因子
    ├─→ ats_core.features.social.calc_social_score()    # S因子
    ├─→ ats_core.features.innovation.calc_innovation()  # I因子
    ├─→ ats_core.factors_v2.funding_v2.calc_F_v2()      # F因子v2
    ├─→ ats_core.factors_v2.balance.calc_balance()      # B因子
    │
    ├─→ ats_core.scoring.scoring_utils.StandardizationChain  # 标准化
    │
    ├─→ ats_core.config.threshold_config.get_thresholds()    # 读取阈值
    ├─→ ats_core.config.factor_config.get_factor_config()    # 读取因子配置
    │
    └─→ return result (包含10因子得分、信号方向、置信度)
```

### v7.2增强流程

```
ats_core.pipeline.analyze_symbol_v72.analyze_with_v72_enhancements(result)
    │
    ├─→ 因子分组
    │   ├─ TC组: Trend + Confluence
    │   ├─ VOM组: Volume + OI + Momentum
    │   └─ B因子: Balance
    │
    ├─→ Gate5: 检查因子分组质量
    │
    ├─→ ats_core.calibration.empirical_calibration.get_calibrated_probability()
    ├─→ Gate6: 统计校准过滤
    │
    ├─→ ats_core.modulators.confidence_modulator.adjust_confidence()
    ├─→ ats_core.modulators.new_coin_modulator.adjust_for_new_coin()
    │
    └─→ Gate7: 综合信心度阈值
```

### 输出与发布流程

```
result (通过所有7道闸门)
    ↓
ats_core.publishing.anti_jitter.AntiJitter.should_publish(symbol, direction)
    ↓ 通过防抖动检查
ats_core.outputs.telegram_fmt.render_trade_v72(result)
    ↓ 格式化为Telegram消息
发送到Telegram频道
    ↓
ats_core.data.analysis_db.save_analysis_result(result)     # 保存到数据库
ats_core.data.trade_recorder.record_trade(result)          # 记录交易
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🎯 10因子 + 7闸门 详解
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 10个核心因子

| 因子 | 名称 | 实现文件 | 说明 | 输出范围 |
|------|------|----------|------|----------|
| **T** | Trend（趋势）| `features/trend.py` | 多周期EMA趋势强度 | -1.0 ~ 1.0 |
| **M** | Momentum（动量）| `features/momentum.py` | RSI、MACD等动量指标 | -1.0 ~ 1.0 |
| **C** | Confluence（汇聚）| `features/confluence.py` | 多周期趋势一致性 | 0.0 ~ 1.0 |
| **V** | Volume（成交量）| `features/volume.py` | 成交量异常、成交量趋势 | 0.0 ~ 1.0 |
| **O** | OI（持仓量）| `features/oi_analysis.py` | 持仓量变化率 | -1.0 ~ 1.0 |
| **B** | Balance（多空平衡）| `factors_v2/balance.py` | 买卖力量对比 | -1.0 ~ 1.0 |
| **F** | Funding（资金费率）| `factors_v2/funding_v2.py` | 资金费率v2精确定义 | -1.0 ~ 1.0 |
| **L** | Liquidity（流动性）| `features/liquidity.py` | 订单簿深度、流动性 | 0.0 ~ 1.0 |
| **S** | Social（社交热度）| `features/social.py` | 推特、Reddit等热度 | 0.0 ~ 1.0 |
| **I** | Innovation（新币溢价）| `features/innovation.py` | 新币年龄、创新性 | 0.0 ~ 1.0 |

### 7道闸门

| 闸门 | 名称 | 实现位置 | 检查内容 | 阈值配置 |
|------|------|----------|----------|----------|
| **Gate1** | 趋势强度 | `analyze_symbol.py` | `abs(T) >= threshold` | `signal_thresholds.json` |
| **Gate2** | 方向一致性 | `analyze_symbol.py` | 各因子与趋势方向一致 | 硬编码 |
| **Gate3** | 动量确认 | `analyze_symbol.py` | `M * T > 0` | 硬编码 |
| **Gate4** | 新币风险 | `analyze_symbol.py` | 新币阈值调整 | `signal_thresholds.json` |
| **Gate5** | 因子分组 | `analyze_symbol_v72.py` | TC/VOM/B组质量 | `signal_thresholds.json` |
| **Gate6** | 统计校准 | `analyze_symbol_v72.py` | 历史准确率 >= 阈值 | `signal_thresholds.json` |
| **Gate7** | 综合信心度 | `analyze_symbol_v72.py` | `confidence >= threshold` | `signal_thresholds.json` |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 📁 文件分类与职责
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 🔴 核心文件（必须理解）- 12个文件

**配置层** (4个)
- `ats_core/cfg.py` - 全局配置入口
- `ats_core/config/threshold_config.py` - 阈值配置管理
- `ats_core/config/factor_config.py` - 因子配置管理
- `ats_core/config/anti_jitter_config.py` - 防抖动配置

**流程层** (3个)
- `ats_core/pipeline/analyze_symbol.py` - 主分析流程（Gate1-4）
- `ats_core/pipeline/analyze_symbol_v72.py` - v7.2增强（Gate5-7）
- `ats_core/pipeline/batch_scan_optimized.py` - 批量扫描器

**输出层** (2个)
- `ats_core/outputs/telegram_fmt.py` - Telegram格式化
- `ats_core/publishing/anti_jitter.py` - 防抖动逻辑

**数据层** (2个)
- `ats_core/data/analysis_db.py` - SQLite数据库
- `ats_core/data/trade_recorder.py` - 交易记录

**工具层** (1个)
- `ats_core/logging.py` - 日志工具

### 🟢 接口文件（只需要知道API）- 47个文件

**数据源** (4个) - `ats_core/sources/`
- 提供市场数据：K线、价格、持仓量等

**因子计算** (19个) - `ats_core/features/` + `ats_core/factors_v2/`
- 计算10个因子的具体实现

**评分系统** (5个) - `ats_core/scoring/`
- 标准化、归一化、评分

**工具函数** (6个) - `ats_core/utils/`
- 数学工具、技术指标、辅助函数

**调节器** (4个) - `ats_core/modulators/`
- 信心度调节、新币调节

**其他** (9个) - `ats_core/calibration/`, `ats_core/analysis/`, `ats_core/execution/`
- 统计校准、分析报告、交易执行

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🔧 配置文件说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 核心配置文件（必须导入Claude Project）

1. **config/signal_thresholds.json** - 信号阈值配置
   ```json
   {
     "gate1_trend_strength": 0.6,
     "gate7_confidence": 0.7,
     "new_coin_multiplier": 1.2
   }
   ```

2. **config/factor_weights.json** - 因子权重配置
   ```json
   {
     "trend": 0.25,
     "momentum": 0.20,
     "confluence": 0.15,
     ...
   }
   ```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 📊 数据流向图
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
[Binance API]
     ↓
[Sources层] → K线、OI、价格
     ↓
[Features层] → 计算10因子
     ↓
[Scoring层] → 标准化、归一化
     ↓
[Pipeline层] → Gate1-4过滤
     ↓
[V72层] → Gate5-7过滤 + 因子分组 + 统计校准
     ↓
[Calibration层] → 经验校准
     ↓
[Modulators层] → 信心度调节
     ↓
[Anti-Jitter层] → 防抖动检查
     ↓
[Telegram层] → 格式化并发送
     ↓
[Data层] → 保存到SQLite
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🎯 关键接口约定
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 因子计算接口

所有因子计算函数遵循统一接口：

```python
def calc_factor(klines, **kwargs) -> Union[float, Dict[str, float]]:
    """
    参数:
        klines: K线数据 List[Dict]
        **kwargs: 其他可选参数（如oi_data, cvd等）

    返回:
        float: 单一因子得分（-1.0 ~ 1.0 或 0.0 ~ 1.0）
        Dict: 复合因子得分（如trend返回多个子得分）
    """
    pass
```

### 闸门检查接口

所有闸门检查遵循统一接口：

```python
def check_gate(result: Dict, thresholds: Dict) -> Tuple[bool, str]:
    """
    参数:
        result: 分析结果字典
        thresholds: 阈值配置

    返回:
        (通过/不通过, 原因说明)
    """
    pass
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 📖 阅读顺序建议
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 第一步：理解入口和主流程（3个文件）
1. `scripts/realtime_signal_scanner.py` - 从这里开始
2. `ats_core/pipeline/batch_scan_optimized.py` - 批量处理逻辑
3. `ats_core/pipeline/analyze_symbol.py` - 核心分析流程

### 第二步：理解配置系统（4个文件）
4. `config/signal_thresholds.json` - 所有阈值
5. `config/factor_weights.json` - 因子权重
6. `ats_core/config/threshold_config.py` - 配置读取
7. `ats_core/config/factor_config.py` - 配置管理

### 第三步：理解v7.2增强（1个文件）
8. `ats_core/pipeline/analyze_symbol_v72.py` - Gate5-7和因子分组

### 第四步：理解输出和发布（2个文件）
9. `ats_core/outputs/telegram_fmt.py` - 信号格式化
10. `ats_core/publishing/anti_jitter.py` - 防抖动

### 第五步：理解数据存储（2个文件）
11. `ats_core/data/analysis_db.py` - SQLite数据库
12. `ats_core/data/trade_recorder.py` - 交易记录

### 第六步：浏览接口文档
13. 阅读 `INTERFACES.md` - 了解所有辅助模块的API

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## ✅ 总结
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. **核心文件**：12个（9,778行代码）→ 完整导入Claude Project
2. **接口文件**：47个（15,578行代码）→ 只记录API到 `INTERFACES.md`
3. **压缩率**：38.6% 的代码量，100% 的功能理解
4. **架构**：分层清晰（入口→批量→分析→因子→闸门→输出→存储）
5. **可维护性**：修改核心逻辑只需要改12个核心文件
