# CryptoSignal 测试目录

## v6.6 当前测试

### ✅ 活跃测试（推荐使用）

#### `test_v66_5coins.py` - v6.6架构快速验证
**用途**: 测试5个主流币种，验证v6.6架构合规性

**测试内容**:
- 6个核心因子 (T/M/C/V/O/B) 范围验证
- 4个调制器 (L/S/F/I) 范围验证
- 废弃因子 (Q/E) 清理验证
- 软约束系统验证
- 详细因子输出验证

**运行方法**:
```bash
cd /path/to/cryptosignal
python3 tests/test_v66_5coins.py
```

**预期输出**:
```
🧪 v6.6架构测试 - 5个币种因子系统验证
✅ 所有测试通过！v6.6架构完全合规
```

---

## 📦 归档测试（历史参考）

以下测试文件针对旧版本架构（v6.0-v6.5），仅供参考：

### `test_5_coins_old.py` (v6.0)
- ⚠️ 过时：仍检查Q因子
- 用途：旧版9+2因子系统测试
- 状态：归档，不推荐使用

### `test_enhanced_output.py` (v6.0)
- ⚠️ 过时：使用旧版telegram_fmt
- 用途：测试四门系统输出
- 状态：归档

### `test_phase2.py` (v6.4)
- ⚠️ 过时：v6.4新币Phase 2架构
- 用途：新币数据流测试
- 状态：归档

### `test_symmetry_fix.py` (v6.0)
- ⚠️ 过时：多空对称性修复验证
- 用途：因子对称性测试
- 状态：归档

### `test_factors_v2.py` (v6.0)
- ⚠️ 部分过时：包含Q因子单元测试
- 用途：因子系统v2单元测试
- 状态：部分可用（CVD/独立性/流动性测试仍有效）
- 需要：跳过清算密度相关测试

### `test_standardization_chain.py` (v6.0)
- ⚠️ 部分过时：包含Q因子标准化测试
- 用途：因子标准化链测试
- 状态：部分可用
- 需要：跳过Q因子测试

### `test_auto_trader.py`
- 用途：自动交易器测试
- 状态：可能需要更新配置

### `verify_standardization_imports.py`
- 用途：验证因子导入
- 状态：需要更新（移除liquidation导入）

---

## 🔄 迁移指南

### v6.0/v6.5 → v6.6 测试更新

**核心变更**:
1. **移除Q因子测试** - 清算密度因子已废弃
2. **移除E因子测试** - 环境因子已废弃
3. **S因子重新分类** - 从A层因子移至B层调制器
4. **更新因子列表**:
   - A层核心因子: `T, M, C, V, O, B` (6个)
   - B层调制器: `L, S, F, I` (4个)
5. **更新权重验证**:
   - 核心因子权重总和 = 100%
   - 调制器权重 = 0%

**示例更新**:
```python
# ❌ 旧版测试（v6.0）
FACTORS = ['T', 'M', 'C', 'S', 'V', 'O', 'L', 'B', 'Q']

# ✅ 新版测试（v6.6）
CORE_FACTORS = ['T', 'M', 'C', 'V', 'O', 'B']
MODULATORS = ['L', 'S', 'F', 'I']
```

---

## 🚀 快速开始

**运行v6.6验证测试**:
```bash
# 测试5个币种（推荐）
python3 tests/test_v66_5coins.py

# 查看详细输出
python3 tests/test_v66_5coins.py 2>&1 | less
```

**测试特定币种**:
编辑 `test_v66_5coins.py` 第26行:
```python
test_symbols = ['YOURTOKEN1USDT', 'YOURTOKEN2USDT', ...]
```

---

## 📝 创建新测试

**v6.6测试模板**:
```python
from ats_core.pipeline.analyze_symbol import analyze_symbol

# v6.6架构定义
CORE_FACTORS = ['T', 'M', 'C', 'V', 'O', 'B']
MODULATORS = ['L', 'S', 'F', 'I']

result = analyze_symbol('BTCUSDT')
scores = result.get('scores', {})
modulation = result.get('modulation', {})

# 验证核心因子
for f in CORE_FACTORS:
    assert f in scores
    assert -100 <= scores[f] <= 100

# 验证调制器
for m in MODULATORS:
    assert m in modulation
    assert -100 <= modulation[m] <= 100
```

---

## 📞 问题反馈

如发现测试问题或需要新测试，请提交issue到项目仓库。
